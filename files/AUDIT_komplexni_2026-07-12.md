# Komplexní audit aplikace — 2026-07-12

Multi-agent audit (19 auditorů × adversariální ověření každého nálezu). **170 potvrzených** nálezů (0 kritických, 16 high, 68 medium, 86 low) napříč 52 soubory. Baseline 458568e.


## apps/web/src/screens/Seznamy.tsx  (11)

- **🔴 HIGH** `Seznamy.tsx:609` — Reset odškrtne celý seznam bez undo a bez potvrzení [data-loss]
  - Scénář: V detailu akce/seznamu, kde je napříč týmem odškrtáno mnoho položek, uživatel omylem klikne na „Reset" v horní liště (žádný confirm). reset() nastaví done=0 pro VŠECHNY položky seznamu (i cizí odškrtnutí) a archived=0. Zobrazí se jen informační showToast; na rozdíl od deleteSection/deleteItem tu chybí pushUndo, takže není žádné ⌘Z ani jiná cesta zpět. Zaškrtnutý postup je nevratně ztracen a přes PowerSync sync i u kolegů. Oprava: před resetem sejmout snímek done (a archived) a zaregistrovat pushUndo({undo,redo}) jako u delete operací, případně přidat potvrzení.
  - Oprava: Před resetem sejmout snímek done stavů (a archived) a zaregistrovat pushUndo({undo,redo}) stejně jako u deleteSection, případně přidat potvrzení.
- **🟡 MED** `Seznamy.tsx:482` — Souběžný sync přepíše rozepsaný název/„event“ v detailu [sync]
  - Scénář: Uživatel A otevře detail seznamu, klikne do inputu názvu (ř. 725) nebo pole event (ř. 749) a začne psát, aniž by opustil pole (bez blur/Enter). Souběžně kolega B tentýž seznam přejmenuje. PowerSync reaktivní dotaz (usePsQuery, ř. 98) dotáhne novou list.name, `selected` (ř. 111) se přepočítá a nový objekt se předá jako prop `list` do ListDetail (ř. 212). useEffect (ř. 482-486, deps [list.id, list.name, list.event]) se spustí kvůli změně primitivní hodnoty list.name a zavolá setName(list.name ?? "") — rozepsaný, dosud neuložený text A je přepsán. Totéž pro pole event. Dopad je omezen na hlavičkové pole (název/event), text je znovu napsatelný; okno je úzké (nutno psát bez blur ve chvíli souběžného syncu téhož seznamu). Vlastní blur-save uživatele A tuto ztrátu nezpůsobí (setName dostane stejnou hodnotu). Oprava: neresetovat lokální stav, když je input fokusovaný/dirty; resetovat jen na změnu list.id, nebo držet dirty flag.
  - Oprava: Nesynchronizovat do lokálního stavu, když je input fokusovaný / dirty; sledovat „dirty“ flag nebo resetovat jen na změnu list.id.
- **🟡 MED** `Seznamy.tsx:168` — Znak „|“ v textu položky korumpuje round-trip přes šablonu [correctness]
  - Scénář: Položka s textem „Grilování 18:00 | u jezera“ se v saveAsTemplate (ř. 630) uloží celá (qty prázdné, pipe se nepřidává), takže blueprint obsahuje `Grilování 18:00 | u jezera`. Při createFromTemplate (ř. 168) `String(...).split("|")` rozdělí na text="Grilování 18:00 " a qty=" u jezera" — text je oříznut a vznikne falešný qty chip. Jakýkoli „|“ v textu položky se rozbije.
  - Oprava: Neserializovat text a qty přes `|`; použít strukturovaný JSON objekt {text, qty} pro položky šablony.
- **🟡 MED** `Seznamy.tsx:137` — Šablona se založí do aktivního týmového prostoru bez ohledu na sféru šablony [correctness]
  - Scénář: Uživatel je v aktivním týmovém prostoru B (activeWs=B, není isPersonal) a má osobní šablonu (tpl.workspace_id = jeho osobní ws, vzniklá přes saveAsTemplate z osobního seznamu). Na obrazovce Seznamy jsou všechny šablony (dotaz bez filtru), takže osobní šablona je viditelná i v týmovém kontextu. Klikne „Použít šablonu". Logika ř. 137-140 preferuje activeWs (protože je týmový) a ignoruje tpl.workspace_id → nový seznam i všechny sekce/položky se vloží do týmu B (wsId u INSERT na ř. 165/172) a jsou sdílené celému týmu, ačkoli šablona byla ze soukromé sféry. Karta šablony sféru neukazuje a chybí volba cíle, takže uživatel destinaci nemůže očekávat. (Sníženo z případného vyššího dopadu: seznam obsahuje jen strukturu z šablony, ne libovolná soukromá data, a jde o vědomé kliknutí — vada je ve špatné cílové sféře, ne v tichém úniku dat.)
  - Oprava: Respektovat sféru tpl.workspace_id (osobní×týmová) nebo se zeptat, do jakého prostoru instanci založit.
- **🟡 MED** `Seznamy.tsx:482` — In-progress list name/event edit is discarded when the row updates via sync [sync]
  - Scénář: In Seznamy list detail, User A focuses the list name (or event) header input and begins typing without yet blurring/pressing Enter, so the edit lives only in local `name`/`event` state. Meanwhile User B (a teammate or another of User A's devices) renames the same list or changes its event. That write syncs into User A's live `lists` PowerSync query (line 98), producing a new `list.name`/`list.event`. Because ListDetail stays mounted and the effect at lines 482-486 depends on [list.id, list.name, list.event], it fires and runs setName/setEvent with the incoming sync value, silently overwriting User A's uncommitted keystrokes. There is no dirty flag or focus check to prevent clobbering an actively edited field.
  - Oprava: Only re-sync local state on list.id change (or when the field is not focused / no pending local edit), not on every list.name/list.event change; compare against a 'dirty' flag before clobbering the input.
- **🟡 MED** `Seznamy.tsx:727` — Vymazání názvu seznamu + odklik nechá titulek vizuálně prázdný až do navigace [state]
  - Scénář: V detailu seznamu smažu obsah inputu názvu (name='') a kliknu jinam. onBlur (ř. 727) volá `saveField('name', ''.trim() || list.name)` = původní název. `saveField` (ř. 515-517) vidí `list.name === val` → return bez zápisu. DB se nezmění → `list` prop se nezmění → useEffect (ř. 482-486, deps [list.id, list.name, list.event]) se nespustí → `setName` se nezavolá → lokální `name` zůstane '' a input zůstane prázdný. Titulek vypadá smazaný, dokud uživatel neopustí a znovu neotevře seznam. Přejmenování sekce tuto chybu NEMÁ, protože `saveSectionName` (ř. 543-547) maže lokální override a spadne zpět na `sec.name`.
  - Oprava: Po revertu synchronizovat lokální stav: v onBlur udělat `setName(list.name ?? '')` když je trim prázdný, nebo řešit jako u sekcí (bez lokálního zrcadlení + fallback na `list.name`).
- **⚪ LOW** `Seznamy.tsx:727` — Vymazání názvu na blur nechá prázdný titulek, ale DB si drží starý — rozjezd UI×DB [state]
  - Scénář: Uživatel na obrazovce Seznamy smaže celý obsah inputu názvu seznamu a klikne jinam (blur). `saveField("name", name.trim() || (list.name ?? ""))` dosadí za prázdnou hodnotu původní `list.name`; guard na ř. 516 rozpozná shodu s DB a nic nezapíše (správně nechrání data). Lokální řízený stav `name` ale zůstane prázdný a synchronizační useEffect (ř. 482) se nespustí, protože `list.name` se v DB nezměnila. Hlavička proto ukazuje prázdný titulek, přestože seznam má v DB pořád původní jméno. Nesoulad UI×DB přetrvá, dokud se komponenta neremountuje (odnavigování a návrat obnoví název). Jde o vizuální/přechodnou vadu bez ztráty dat. Oprava: v onBlur při prázdné hodnotě explicitně obnovit input `setName(list.name ?? "")`.
  - Oprava: Při prázdném názvu explicitně obnovit input: onBlur nastavit setName(list.name ?? "") při prázdné hodnotě.
- **⚪ LOW** `Seznamy.tsx:626` — Opakované „Uložit jako šablonu“ tvoří duplicitní šablony [state]
  - Scénář: Double-clicking or rapidly clicking "Uložit jako šablonu" runs saveAsTemplate multiple times; each call inserts a new list_templates row (unique UUID, name `${name} ${tplSuffix}`) with no in-flight guard and no name/content dedup or unique constraint. The template gallery accumulates identical copies that sync to the whole workspace. Impact is cosmetic clutter (user can delete duplicates) — no data loss, corruption, or security issue — and matches the codebase-wide pattern of action buttons (reset, archive) lacking in-flight guards, hence low rather than medium.
  - Oprava: Během zápisu tlačítko disablovat (in-flight flag) a/nebo deduplikovat podle názvu/obsahu.
- **⚪ LOW** `Seznamy.tsx:612` — Reset tiše odarchivuje seznam [correctness]
  - Scénář: U archivovaného seznamu uživatel klikne „Reset“, aby vyčistil odškrtnutí pro příští použití. reset() ale zároveň nastaví archived=0 (ř. 612), takže seznam bez upozornění zmizí z Archivu a objeví se zpět v aktivních skupinách. Chování možná záměrné, ale v UI nijak nenaznačené.
  - Oprava: Oddělit „odškrtnout“ od „obnovit z archivu“, nebo vizuálně naznačit, že Reset i odarchivuje.
- **⚪ LOW** `Seznamy.tsx:322` — Prázdný stav a zakládací tlačítka skupin se zobrazují současně [empty-state]
  - Scénář: Uživatel je členem aspoň jednoho týmu a má nula aktivních (nearchivovaných) seznamů — typicky čerstvý účet nebo po archivaci všech seznamů. Protože myPersonalWs (osobní inbox, R8) i teamTargetWs jsou non-null, obě skupiny listGroup se vykreslí i s prázdným polem — zobrazí nadpisy „Osobní"/„Týmové" a dashed tlačítka „+ Nový seznam". Nad nimi zároveň běží prázdný stav lists.emptyActive. Výsledek: hláška „nic tu není" a rovnou pod ní strukturované skupiny se zakládacími tlačítky — redundantní/protichůdné sdělení.
  - Oprava: Skrýt emptyActive, když se zobrazuje aspoň jedno zakládací tlačítko, nebo prázdný stav integrovat do skupin.
- **⚪ LOW** `Seznamy.tsx:810` — Přiřazená položka na bývalého člena vypadá jako nepřiřazená [correctness]
  - Scénář: Položka má who_id nastavené na uživatele, který není v aktuálním rosteru `team` — buď proto, že opustil workspace (members API ho nevrací), nebo protože fetch členů ještě běží (team === undefined při prvním renderu každého načtení). `who` (ř. 810) je undefined → avatar (ř. 881) zobrazí „+" shodně s nepřiřazenou položkou a tooltip (ř. 867) ukáže obecný „assign" popisek. V assign popoveru se navíc nic nezvýrazní (ř. 929–931 porovnávají who_id s existujícími členy), takže uživatel nepozná, že položka je přiřazena, a případné nové přiřazení přepíše původní who_id.
  - Oprava: Pro neznámého/nenačteného assignee zobrazit odlišný placeholder (např. „?“) místo shodného „+“.

## apps/web/src/screens/Reporty.tsx  (9)

- **🔴 HIGH** `Reporty.tsx:227` — Per-osobní done/open/eff se počítá z tasks.completed_at, ignoruje assignments.completed_at → R2 sdílené úkoly zkreslené [invariant]
  - Scénář: R2 shared_all úkol se dvěma+ přiřazenými (shared_all je dle R2 výchozí pro hromadné/neinteraktivní přiřazení). Člen A svou účast dokončí (assignments.completed_at nastaveno), člen B ještě ne → tasks.completed_at zůstává null (nastaví se odvozeně až po dokončení všech). V Reporty > Lidé memberStats (Reporty.tsx:227-236) filtruje 'mine' z assigneesByTask, ale 'done'/'open' odvozuje z tk.completed_at (task-level), NE z účasti dané osoby. Důsledek pro A: úkol veden jako 'open' místo 'done', efektivita (eff) podhodnocená, a má-li úkol termín v minulosti, je A ř. 230-232 chybně započítán jako 'overdue', ačkoli svou část splnil. Oprava: do dotazu na assignments (ř. 74) přidat completed_at a per-osobní done/open/overdue počítat z účasti osoby (a.completed_at) — POZOR: současně řešit shared_any, kde assignments.completed_at není plněn, jinak by se shared_any úkoly zobrazovaly jako nikdy nedokončené.
  - Oprava: Načíst assignments.completed_at a per-osobní staty počítat z účasti dané osoby (a.completed_at), ne z task-level completed_at.
- **🟡 MED** `Reporty.tsx:130` — completed_at (UTC ISO) porovnáváno slice(0,10) s lokálním datem dne → úkol spadne do špatného dne/týdne [i18n]
  - Scénář: Reporty.tsx ř. 130: `tk.completed_at?.slice(0,10)` je UTC datum (zapsané `new Date().toISOString()` v lib/tasks.ts), ale porovnává se s lokálními daty z `weekDays()`/`todayISO()`. Úkol dokončený v okně lokální půlnoc až UTC offset (v ČR 00:00–01:00 v zimě, 00:00–02:00 v létě) dostane completed_at o den dříve v UTC → v týdenním grafu spadne do předchozího dne; jde-li o dnešní úkol, jeho odvozený den patří do minulého týdne a z `perDay`/`weekDone`/`avg` vypadne úplně. Oprava: převést completed_at na lokální YYYY-MM-DD (new Date(completed_at) → lokální gettery) před porovnáním, konzistentně s todayISO()/weekDays().
  - Oprava: Převádět completed_at na lokální datum před slice (např. new Date(completed_at) → lokální YYYY-MM-DD), stejně jako todayISO()/weekDays() používají lokální čas.
- **🟡 MED** `Reporty.tsx:688` — MemberDetail nemá Esc handler, scroll-lock ani focus-trap (na rozdíl od sourozeneckých overlayů) [overlay-esc]
  - Scénář: Uživatel otevře detail člena v Reportech (MemberDetail, fixed panel zIndex 41). Zmáčkne Esc → nic se nestane, protože komponenta neregistruje žádný keydown listener (na rozdíl od NotifCenter:197, TaskDetailPanel:185 a PeekPanel:66, které Esc obsluhují). Zavřít lze jen kliknutím na X nebo backdrop. Chybí také focus-trap/autofocus (který TaskDetailPanel má přes useFocusTrap), takže Tab bloudí po skryté stránce za panelem — nekonzistentní a11y chování napříč overlayi. POZN.: původní tvrzení o chybějícím scroll-locku jako nekonzistenci neplatí — žádný sourozenecký overlay scroll pozadí nezamyká, takže rolování podkladu je stejné chování jako jinde, ne odchylka.
  - Oprava: Přidat useEffect s keydown→Escape=onClose, body overflow:hidden po dobu otevření a přesun/uvěznění focusu do panelu.
- **🟡 MED** `Reporty.tsx:831` — Přepínač role bez isPending/optimistic → dvojité odeslání a žádná okamžitá odezva [state]
  - Scénář: Na kartě člena (ne-owner) uživatel rychle dvojklikne / několikrát klikne na segmenty rolí (admin/member/guest). Protože onClick=roleMut.mutate(k) nemá disabled ani guard na roleMut.isPending, každý klik odešle samostatný PATCH na /api/workspaces/{id}/members/{memberId}/role. Zvýraznění aktivního segmentu je vázáno na roleOf odvozené z member.role a bez optimistic update (chybí onMutate) se nezmění, dokud nedoběhne PATCH + invalidateQueries + refetch — při pomalé/offline síti tedy uživatel nevidí žádnou okamžitou odezvu a klikne znovu. Výsledkem jsou duplicitní/závodící requesty na privilegovaný role endpoint; pořadí odpovědí není zaručeno, takže může dočasně vzniknout nezamýšlená role. Oprava: disabled={roleMut.isPending}, ignorovat klik na již aktivní roli a přidat lokální pending/optimistický highlight do doběhnutí mutace.
  - Oprava: Zablokovat tlačítka při roleMut.isPending a přidat optimistický highlight (nebo lokální pending role) do doběhnutí mutace.
- **⚪ LOW** `Reporty.tsx:150` — Průměr/den dělí natvrdo 5, ale weekDone sčítá i víkend → nekonzistentní přeceněný průměr [correctness]
  - Scénář: Uživatel odškrtne úkol(y) v sobotu/neděli aktuálního týdne (completed_at = víkendové datum). weekDays() zahrne tento den mezi 7 dnů, perDay ho spočítá, weekDone ho přičte. avg = weekDone/5 tak zahrne víkendová dokončení do čitatele, ale dělí 5 pracovními dny → nadhodnocený průměr na pracovní den (např. 10 hotových vč. 2 o víkendu → 10/5 = 2,0 místo 8/5 = 1,6). Zároveň KPI „týden hotovo" a 7sloupcový graf ukazují víkend, takže si s dělitelem 5 protiřečí. Dopad je pouze kosmetický (zobrazená hodnota KPI), bez funkčního rozbití.
  - Oprava: Buď sčítat weekDone jen z pracovních dnů (index 0–4) shodně s dělitelem 5, nebo dělit skutečným počtem dnů se záznamem.
- **⚪ LOW** `Reporty.tsx:150` — Desetinná čárka v průměru natvrdo (.replace('.',',')) ignoruje jazyk [i18n]
  - Scénář: Switch UI to English (i18n en). Complete e.g. 26 tasks across the current week in a team workspace. The "avg / day" KPI computes 26/5 = 5.2 but renders "5,2" due to the hardcoded `.replace(".", ",")`, which an English reader parses as a thousands separator. Fix: format with `new Intl.NumberFormat(i18n.language, { maximumFractionDigits: 1 }).format(weekDone / 5)`.
  - Oprava: Použít Intl.NumberFormat(i18n.language, { maximumFractionDigits: 1 }) místo ručního replace.
- **⚪ LOW** `Reporty.tsx:385` — Chybějící prázdné stavy: žádné projekty / žádné cíle / žádní členové [empty-state]
  - Scénář: Nový/prázdný workspace nebo workspace bez týmových cílů: (1) karta „Podle projektu" při wsProjects=[] ukáže jen nadpis a prázdné tělo; (2) karta „Cíle" při reportGoals=[] (běžné — cíle volitelné, filtr na scope=team) ukáže jen nadpis + tlačítko „Všechny cíle" bez řádků; (3) tab „Lidé" při selhání team query (r.ok=false → throw → members=[]) ukáže prázdný roster; jediná zpětná vazba je hint „0 lidí · klikni na kartu…", což je zavádějící a není to skutečný empty/error stav. Doporučení: doplnit textové prázdné stavy do všech tří sekcí a ošetřit error z team query.
  - Oprava: Doplnit textové prázdné stavy pro každou sekci (např. 'Zatím žádné dokončené úkoly / žádné cíle / žádní členové') a ošetřit chybu team query.
- **⚪ LOW** `Reporty.tsx:126` — Aktuální týden zmražen useMemo(weekDays, []) → po půlnoci/přechodu týdne neaktuální graf [state]
  - Scénář: /reporty stays mounted continuously across a week boundary (Sun 23:59 → Mon; e.g. an always-open dashboard/kiosk tab). `days` remains last week's Mon–Sun. When any task change fires the PowerSync live query, `overview` recomputes: `overdue` uses the fresh `todayISO()` (new day, correct), but the weekly bar chart, `weekDone` KPI, and `avg` all use the frozen `days` array and keep showing the PREVIOUS week — the graph never advances to the new week and is inconsistent with the overdue KPI. Navigating away and back to /reporty remounts and self-heals. Fix: derive `days` with a dependency that changes at the day/week boundary (e.g. include `todayISO()` as a dep or a periodic refresh) instead of `[]`.
  - Oprava: Přepočítávat weekDays na základě aktuálního dne (např. závislost na todayISO() nebo periodické obnovení), ne jednorázově [] .
- **⚪ LOW** `Reporty.tsx:850` — Seznam úkolů člena tvrdě oříznut na 10 bez indikace přetečení [empty-state]
  - Scénář: V panelu detailu člena v Reportech: má-li člen více než 10 otevřených úkolů (např. 30), nadpis „Úkoly · 30" (ř. 848) ukazuje celkový počet, ale `stats.open.slice(0, 10)` (ř. 850) vykreslí jen prvních 10 řádků. Po mapě (konec ř. 903) následuje rovnou sekce „podle projektu", takže neexistuje žádný indikátor přetečení ani afordance („+20 dalších"/rozbalení/odkaz na /ukoly), kterou by šlo zbylých 20 úkolů z tohoto panelu zobrazit. Nadpis s celkovým počtem částečně signalizuje neúplnost, proto low.
  - Oprava: Přidat řádek/odkaz '+N dalších' vedoucí na filtrované /ukoly, nebo tlačítko rozbalení.

## apps/web/src/components/TaskDetailPanel.tsx  (8)

- **🔴 HIGH** `TaskDetailPanel.tsx:376` — useEffect([task]) přepíše rozepsaný název/popis při každé změně řádku (i vzdálené sync) [sync]
  - Scénář: Uživatel A má otevřený detail úkolu a aktivně píše do pole názvu nebo do rozbaleného textarea popisu (input je fokusnutý, blur ještě neproběhl, patchLog se pouští až na onBlur). Uživatel B (nebo background sync) v tu chvíli změní přes sync jakýkoli sloupec téhož úkolu (status/priorita/done) nebo jiný řádek tabulky tasks v synchronizovaném projektu. PowerSync watched query znovu emituje nový objekt `task`; useEffect s deps `[task]` (ř. 376-381) bez kontroly dirty/fokusu zavolá setName(task.name)/setDesc(task.description) a přepíše rozepsaný neuložený text hodnotou z DB. Uživatel A ztratí rozepsaný název/popis uprostřed psaní. Oprava: deps `[task?.id]` nebo seedovat jen když input není fokusovaný / dirty flag.
  - Oprava: Změnit deps na `[task?.id]` (reseed jen při přepnutí úkolu), nebo seedovat stav jen když input není fokusovaný / dirty flag. Případně řízeně mergovat jen sloupce, které se nedotýkají editovaného pole.
- **🔴 HIGH** `TaskDetailPanel.tsx:461` — Per-osobní zaškrtnutí v shared_all neodvodí dokončení úkolu (R2) [invariant]
  - Scénář: Režim shared_all, ≥1 přiřazená osoba. Uživatel (např. manažer v detailu úkolu) odškrtne všechny osoby přes kulaté per-osobní checkboxy (BrassCheck → togglePersonDone, ř. 461–465). Všechny assignments.completed_at se naplní, ale tasks.completed_at zůstane null → done=Boolean(task.completed_at) je stále false, úkol je vizuálně „všichni hotovi" (zelené fajfky, hint X/Y), přesto se tváří jako nedokončený. Nedojde k odvozenému dokončení dle R2, neposune se řetězec postupu (advanceChainForTask se nevolá), nesynchronizuje se status_id (Board zůstane mimo done sloupec) a zkreslí se statistiky (Přehled/Velín/Reporty). Navíc togglePersonDone nepushuje undo, takže tyto změny nelze vrátit ⌘Z. Chyba je tichá a reprodukovatelná pokaždé, když poslední dokončení projde per-osobním checkboxem místo hlavního/patičkového tlačítka.
  - Oprava: V togglePersonDone po zápisu spočítat, zda jsou všechny assignments completed_at != null, a odvozeně nastavit tasks.completed_at + status (resolveStatusForDone) + advanceChainForTask, symetricky při odškrtnutí. Přidat pushUndo. Lépe sdílet logiku s toggleTask.
- **🟡 MED** `TaskDetailPanel.tsx:913` — Přímá editace termínu u opakovaného úkolu posune kotvu celé řady bez dotazu (R4) [invariant]
  - Scénář: Otevřu detail opakovaného BASE úkolu (recurrence_rule != null) jeho vlastním id (ne konkrétní výskyt), takže occ=null a editační sekce Vlastnosti je viditelná (editOpen default true). Změním datum v `<input type="date">` (ř. 910-916) → onChange (ř. 913) → patchLog({due_date}) → patch(realId,{due_date}) posune kotvu CELÉ řady bez dialogu tento/tento a další/celá řada. Sousední tlačítka Zítra/Příští pondělí tuto akci pro recurrence_rule blokují guardem (ř. 932-935). Opravená závažnost medium: změna je vratná přes undo toast v patchLog, jde o kanonické pole base úkolu (ne výskyt) a ani guardovaná cesta plný R4 dialog neimplementuje (jen odmítne) — jde o konzistentní invariantní mezeru, ne o ztrátu dat. Oprava: sdílet jeden helper mezi date/time inputem a quick-shift tlačítky, který u recurrence_rule buď odmítne (toast) nebo otevře výběr rozsahu řady.
  - Oprava: Do onChange date inputu (a případně time/start_date) přidat stejný guard jako u quick-shift tlačítek: u task.recurrence_rule buď zobrazit toast a přerušit, nebo otevřít dialog výběru rozsahu řady. Ideálně sdílet jednu helper funkci mezi oběma cestami.
- **🟡 MED** `TaskDetailPanel.tsx:471` — Dvojí Enter v poli podúkolu vloží duplicitní podúkol [state]
  - Scénář: addSub (ř. 469-476) je async: kontrola `subText.trim()` → await INSERT → teprve pak setSubText(""). onKeyDown={Enter && void addSub()} (ř. 1375). Dvě rychlá stisknutí Enter: první addSub začne await INSERT (yield), druhé se spustí dřív, než se stav vyprázdní (setSubText běží až po awaitu), subText je pořád vyplněný → vloží se druhý identický podúkol. Stejný vzor u addCmt (ř. 477-490 + onKeyDown ř. 1537) → duplicitní komentář.
  - Oprava: Vyprázdnit vstup synchronně před awaitem (uložit hodnotu do lokální proměnné, hned setSubText(""), pak INSERT), nebo přidat in-flight guard.
- **🟡 MED** `TaskDetailPanel.tsx:527` — Smazat z ⋯ menu nad výskytem řady smaže CELOU řadu, ne výskyt [data-loss]
  - Scénář: Otevřu konkrétní výskyt opakovaného úkolu (id = base@ISO, occ != null). Header ⋯ menu je vykreslené i v occ režimu bez podmínky !occ; položka Smazat volá del() → deleteTaskWithUndo(realId), kde realId = base úkol, a deleteTasksWithUndo smaže base úkol + všechny potomky v jedné transakci = celou opakovanou řadu. Přitom jediná zamýšlená per-výskyt akce je oddělené tlačítko „Přeskočit". Žádné potvrzení ani toast; uživatel očekává smazání jednoho výskytu, ale zmizí celá řada. Data jsou obnovitelná přes ⌘Z, ale undo není při mazání nijak nabídnuto, takže mismatch není zjevný.
  - Oprava: V occ režimu buď skrýt/zakázat Smazat v ⋯ menu, nebo nabídnout volbu „smazat tento výskyt (skip)" vs „smazat celou řadu" s potvrzením.
- **🟡 MED** `TaskDetailPanel.tsx:439` — logActivity("completed") zapíše „označil hotovo" i když se úkol reálně nedokončil [correctness]
  - Scénář: Otevři detail ZÁKLADNÍHO opakovaného úkolu (prostým id, ne výskyt → panel occ=null) a klikni na hlavní checkbox „Hotovo". toggleDone (ř. 439) zapíše do task_activity field="completed", new_value="1", pak toggleTask (tasks.ts:216-262) jen posune due_date na příští výskyt (completed_at zůstává null). Historie tak hlásí „označil(a) hotovo", ačkoli se úkol jen posunul. Analogicky u shared_all s mojí účastí (ř. 183-210): klik dokončí jen moji účast, tasks.completed_at zůstává null dokud nejsou hotoví všichni, ale audit log už zaznamenal dokončení celého úkolu. Oprava: logovat „completed" až podle skutečné změny tasks.completed_at (nechat log na toggleTask, který výsledek zná), nebo pro opakované/shared_all logovat jiný typ události (posun řady / moje účast).
  - Oprava: Logovat completed až podle skutečné změny tasks.completed_at (nechat logování na toggleTask, který zná výsledek), nebo pro opakované/shared_all logovat jiný typ události (posun řady / moje účast).
- **⚪ LOW** `TaskDetailPanel.tsx:620` — Čas „Uloženo ✓ HH:MM" natvrdo formátován v české locale [i18n]
  - Scénář: Uživatel přepne jazyk na EN v Nastavení (i18n.changeLanguage("en")). Otevře detail úkolu a provede úpravu → objeví se indikátor „Saved ✓ HH:MM". Čas je vykreslen přes toLocaleTimeString("cs", …) s napevno „cs" (ř. 620), takže se zobrazí v české 24h konvenci (např. 14:30) místo očekávaného en-US formátu (2:30 PM). Komponenta má k dispozici jen `t` (ř. 168), `i18n` není destrukturováno.
  - Oprava: Použít i18n.language místo literálu "cs" (nebo undefined pro locale runtime).
- **⚪ LOW** *(uncertain)* `TaskDetailPanel.tsx:501` — Duplikace opakovaného úkolu přenese doneCount → kopie je uprostřed počítané řady [correctness]
  - Scénář: Recurring task with endKind:"count" (e.g. count=5) ticked off several times so recurrence_rule.doneCount>0 and due_date has advanced in lockstep. Duplicating it (copyOne) clones both doneCount and the advanced due_date verbatim, so the copy is positioned at the same point in the series and completes its remaining occurrences rather than a full new count of 5. This is a faithful clone of the object's current state (due_date also inherited), consistent with the stated "copy the whole object" intent, so whether it is a defect depends on undefined product intent about what "duplicate" of a counted series should mean.
  - Oprava: Při kopii recurrence_rule vynulovat doneCount (JSON.parse → doneCount=0 → stringify), případně i normalizovat until/count vůči novému due_date.

## apps/web/src/components/PeekPanel.tsx  (7)

- **🔴 HIGH** `PeekPanel.tsx:99` — PeekPanel overlay missing data-esc-layer → kbNav shortcuts leak to list behind the peek card [keyboard]
  - Scénář: On /ukoly (or /dnes, /nadchazejici) in list view, keyboard-select a task row with j/k. Open the header bell (NotifCenter) and click a mail notification item; MailPeek opens over the screen at layer 66 while NotifCenter stays open behind it. Because neither PeekPanel nor NotifCenter sets data-esc-layer and openId is null, kbNav's guards (kbNav.tsx:27-29) all pass and focus is not in a text field (it rests on the clicked notif button). Pressing Backspace/Delete deletes the underlying selected task (undo-able), Enter opens its detail modal, Space toggles its completion — all silently, on the wrong entity behind the peek. BulkBar's Esc/shortcut handler leaks the same way. Fix: set data-esc-layer on PeekPanel's outer portal element.
  - Oprava: Set data-esc-layer on the PeekPanel outer portal element so kbNav and BulkBar's Esc handler recognize it as an active layer; PeekPanel already relies on that attribute for nested layers so it should also advertise it.
- **🟡 MED** `PeekPanel.tsx:99` — Peek dialog nemá focus trap ani přesun fokusu; pozadí není inert [a11y]
  - Scénář: `<section role="dialog">` se otevře přes scrim, ale fokus zůstává na spouštěcím prvku na Přehledu, chybí aria-modal a background inert. Klávesnicoví/čtečkoví uživatelé mohou Tabem procházet prvky pod scrimem (karty Přehledu), které jsou vizuálně zakryté, a aktivovat je. Esc navíc zavírá jen když neexistuje jiná esc-vrstva, ale fokus se nikdy do dialogu nepřesune.
  - Oprava: Po otevření přesunout fokus do dialogu, přidat aria-modal=true, uzavřít fokus (focus trap) a označit pozadí inert/aria-hidden.
- **🟡 MED** `PeekPanel.tsx:959` — Akční prvky v MailPeek jsou <span onClick>, nikoli klávesnicově ovladatelná tlačítka [a11y]
  - Scénář: Klávesnicový uživatel / čtečka otevře MailPeek: akce Odeslat a Odeslat a Hotovo (ř. 959,966), chipy vlastníka (ř. 562,582) i ghost akce Hotovo/Odložit/→úkol (ř. 626,637,648) jsou `<span onClick>` bez role/tabIndex/onKeyDown — nejsou fokusovatelné ani spustitelné klávesnicí, takže mail nelze v peeku odeslat ani odbavit bez myši. (Zpřesnění: chatovou zprávu lze odeslat přes Enter v inputu na ř. 814, span „Poslat" na ř. 834 je ale rovněž jen span.) Cílová plocha chipů (padding 3px) je navíc pod 24px. Oprava: použít <button type=\"button\"> nebo doplnit role=\"button\", tabIndex=0 a onKeyDown Enter/Space a zvětšit klikací plochu.
  - Oprava: Použít <button type="button"> (nebo přidat role="button", tabIndex=0 a onKeyDown Enter/Space) a zvětšit klikací plochu.
- **⚪ LOW** `PeekPanel.tsx:1112` — DayPeek (denní agenda) ignoruje filtr firmy [correctness]
  - Scénář: Na obrazovce Přehled uživatel vybere firmu ve firmovních chipech (Prehled.tsx:397–404), čímž se zúží všechny karty (včetně „Dnes") na danou firmu a skryjí inbox i spící kroky. Poté klikne na den v kalendářovém widgetu → otevře se DayPeek (PeekPanel.tsx:1112), jehož dotaz `SELECT * FROM tasks WHERE substr(due_date,1,10)=?` filtr firmy nezná. Denní agenda proto zobrazí úkoly všech firem, případně osobní inbox a spící kroky postupů se stejným termínem, tj. širší množinu než zbytek zúžené obrazovky. Bez dopadu na přístup k datům (R5), jde o nekonzistenci scopingu/UX. Oprava: protáhnout aktivní `firm` do PeekTarget/DayPeek a filtrovat řádky dle workspace projektu (a pro plnou paritu s kartou „Dnes" vyloučit inbox a spící/čekající kroky).
  - Oprava: Protáhnout aktivní `firm` do PeekTarget/day a filtrovat řádky podle workspace projektu; případně vyloučit inbox/dormant kroky pro paritu s kartou Dnes.
- **⚪ LOW** *(uncertain)* `PeekPanel.tsx:478` — Dvojité odeslání mailu v MailPeek (tlačítka nejsou blokována během odesílání) [state]
  - Scénář: On a plain thread (no t.coll, no attachment-promise text, no shared-draft approval), a user performs an extremely fast double-click on the 'Odeslat' span. IF the second click's handler executes before React flushes click 1's passive effect (line 451) and unmounts the peek, trySend->checkSend->doSend runs a second time; doSend's fallback (drafts[id]?.text?.trim() || t.draft.join) uses the seed draft after the first send cleared drafts, appending a second identical message to the local sentX array. Both appends are covered by the 10s undo window. In the common case React flushes the passive effect and unmounts the button before the second click lands, preventing the second send; the double-Enter variant is not reachable because the spans have no keyboard handler.
  - Oprava: Přidat lokální `sending` guard (disable/blokace) do trySend a checkSend, ideálně deaktivovat tlačítka po prvním kliknutí do zavření.
- **⚪ LOW** `PeekPanel.tsx:1265` — MemberPeek počítá „dnešek" z UTC místo lokálního data [correctness]
  - Scénář: `const tdy = new Date().toISOString().slice(0,10)` je UTC datum, zatímco zbytek appky používá lokální todayISO(). due_date je naivní lokální. Kolem půlnoci v UTC+2: v 00:30 lokálně (12.7.) je UTC 11.7. → tdy='2026-07-11'; úkol s due '2026-07-11' (lokálně už po termínu) se `over` (ř. 1293) vyhodnotí jako NEpo termínu a nezbarví červeně. Off-by-one oproti kartám na Přehledu.
  - Oprava: Použít todayISO() z lib/tasks místo toISOString().slice(0,10).
- **⚪ LOW** `PeekPanel.tsx:454` — Undo toast v mail peeku zmizí po 5 s, ačkoli komentář slibuje 10s okno [correctness]
  - Scénář: Odešlu mail z peeku (MailPeek). Efekt na ř. 451–461 zavolá showToast s akcí „Zpět" (undoBack) a hned onClose() — peek se zavře, undo lišta z MailThread se nezobrazí. ActionToast (toast.tsx:33) drží toast s akcí jen 5000 ms, ale mailové undo (state.tsx:655, left:10) běží 10 s. Mezi 5.–10. sekundou je tlačítko „Zpět" pryč, ačkoli undoBack by ještě fungoval. Komentář na PeekPanel.tsx:454 slibuje „stejné undo okno jako vlákno (10 s)", což je v rozporu s reálnou 5s viditelností afordance.
  - Oprava: Sjednotit dobu: buď prodloužit action toast na 10 s, nebo v peeku nastavit vlastní TTL/použít samostatný undo pruh navázaný na `m.undo.left`.

## apps/web/src/components/NotifCenter.tsx  (5)

- **🔴 HIGH** `NotifCenter.tsx:281` — Notifikační centrum nenese data-esc-layer → klávesová navigace seznamu maže/edituje úkoly pod otevřeným panelem [keyboard]
  - Scénář: Na Dnes/Úkoly/Nadcházející (list) vyber řádek klávesami j/k (nastaví se kbSel), pak klikni na zvonek v hlavičce → otevře se panel notifikací (portál bez data-esc-layer, bez openId, fokus zůstává na zvonku = button). Stisk Backspace/Delete smaže vybraný úkol pod panelem (deleteTaskWithUndo — vratné, ale neviditelné), 1-4 přepíše prioritu, Space odškrtne — vše bez viditelné odezvy, protože uživatel kouká do notifikací. Oprava: přidat data-esc-layer na scrim i kořen panelu (jako mail/NotifCenter.tsx:135), čímž se zároveň umlčí BulkBar i TaskDetailPanel keyboard.
  - Oprava: Přidat data-esc-layer na kořenový prvek panelu (a scrim) v NotifCenter, stejně jako to má mail/NotifCenter.tsx (ř. 135), CommandPalette (181) a Cheatsheet (60). Tím se současně umlčí i BulkBar (ř. 153) a TaskDetailPanel keyboard.
- **🔴 HIGH** `NotifCenter.tsx:281` — NotifCenter overlay missing data-esc-layer → kbNav Backspace/Space/Enter/1-4 hit the task behind it [keyboard]
  - Scénář: On /ukoly (list view), /dnes, or /nadchazejici, press j to kb-select a task row (kbSel is stored in useKbNav state and survives opening the bell, since the screen is not unmounted). Click the header bell to open the global NotifCenter (Header.tsx:381). Because components/NotifCenter.tsx's scrim/dialog lack data-esc-layer and opening sets no openId, kbNav.tsx does not bail. Press Backspace/Delete → deleteTaskWithUndo(cur) deletes the hidden selected task; Space toggles it done; 1-4 rewrite its priority; Enter opens its detail. These mutate a task the user cannot see behind the panel. Deletion/priority are undoable via undo log + toast, but the done-toggle and priority changes are silent, unintended writes. Fix: add data-esc-layer to the NotifCenter portal container (matching mail/NotifCenter.tsx:135 and all other overlays).
  - Oprava: Add data-esc-layer to the NotifCenter portal container (and ideally trap/stop keydown) exactly like CommandPalette/Cheatsheet/AddTaskModal/mail overlays do, so kbNav (and BulkBar Esc) treat the open notif panel as a blocking layer.
- **🟡 MED** `NotifCenter.tsx:41` — writeSeen volá localStorage.setItem bez try/catch — pád při quota/soukromém režimu [data-loss]
  - Scénář: Uživatel má localStorage v restriktivním/plném stavu (zaplněná kvóta jiným kódem → QuotaExceededError, nebo blokovaný/zakázaný storage → SecurityError, případně Safari private mode s nulovou kvótou). Modul NotifCenter se načte, protože import-time čtení seenCache je v try/catch. Uživatel klikne na notifikaci (quickAct/moduleAct) nebo na „označit vše": prvním krokem je markSeen → writeSeen → localStorage.setItem, které synchronně vyhodí výjimku. Výjimka opustí onClick handler dřív, než se provede switch s akcí, takže detail úkolu se neotevře, mail peek nenaskočí ani nedojde k navigaci — kliknutí na notifikaci je bez efektu. Oprava: obalit setItem ve writeSeen do try/catch, při selhání aktualizovat jen in-memory seenCache a notifikovat subscribery, aby UI fungovalo dál.
  - Oprava: Obalit setItem v writeSeen do try/catch; při selhání aktualizovat jen in-memory seenCache a notifikovat subscribery, aby UI dál fungovalo.
- **⚪ LOW** `NotifCenter.tsx:44` — seenCache v localStorage roste neomezeně, nikdy se nečistí [perf]
  - Scénář: markSeen/writeSeen (NotifCenter.tsx:39–47) pouze přidávají klíče do watson.notifSeen a nikdy je neodstraňují ani neprořezávají proti aktuálně odvozeným items. Klíče `task:<id>` z reálných synced úkolů po termínu se hromadí navždy i po dokončení úkolu, takže mapa v localStorage roste monotónně bez úklidu. Reálný dopad je nízký (drobné bobtnání o desítky až stovky bajtů na klíč); scénář s QuotaExceeded je spekulativní a v praxi nedosažitelný. Vhodná oprava: při zápisu prořezat klíče neodpovídající žádné aktuální položce, případně TTL.
  - Oprava: Při zápisu prořezávat klíče, které už neodpovídají žádné aktuální položce (nebo držet seen jen pro aktuálně odvozené items s TTL).
- **⚪ LOW** `NotifCenter.tsx:196` — Esc v notifikačním centru zavře i CommandPalette otevřenou přes ⌘K [overlay-esc]
  - Scénář: On a non-mail screen with no task detail/peek open, open the notification bell (NotifCenter panel, z-65). Press ⌘K to open CommandPalette (z-73, data-esc-layer) over it. Press Esc once: KeyboardProvider closes the palette AND NotifCenter's unconditional document-level handler simultaneously closes the panel — both overlays vanish on one keystroke, though the user expected only the top palette to close. Fix by adding `if (document.querySelector('[data-esc-layer]')) return;` in the handler, matching the established pattern. Impact is minor (extra overlay closes; easily recovered by reopening the bell), hence severity lowered from medium to low.
  - Oprava: V handleru přidat `if (document.querySelector('[data-esc-layer]')) return;` (peek/openId už řešeno v deps, ale palety/taháky nesou data-esc-layer).

## apps/web/src/mail/state.tsx  (5)

- **🔴 HIGH** `state.tsx:622` — Prázdný/zahozený editor odešle AI draft (fallback t.draft) a hlídač přílohy kontroluje jiný text, než se odešle [correctness]
  - Scénář: Ve vlákně s AI návrhem (t.draft) kliknu Zahodit (setDraft(id,'','empty')). Editor je prázdný, žádný guard neblokuje tlačítko Odeslat. Kliknu Odeslat → checkSend spočítá text přes `?? ` a dostane '' (nullish nechá '' projít), regex na přílohu tedy běží nad '' a nespustí varování, i kdyby zahozený t.draft slova o příloze obsahoval. checkSend pak zavolá doSend, který spočítá text přes `|| ` — ''.trim() je falsy, takže odešle celý zahozený AI návrh t.draft. Výsledek: (1) odešle se obsah, který uživatel právě explicitně zahodil, (2) hlídač přílohy validoval jiný text (''), než se reálně poslal (t.draft), (3) u vláken bez t.draft odejde mail s prázdným tělem, protože chybí guard proti prázdnému odeslání. Dopad tlumí 10s undo lišta a demo povaha modulu (jen lokální sentX).
  - Oprava: Sjednotit výpočet odesílaného textu v checkSend i doSend (obojí nullish nebo obojí trim) a NEfallbackovat na t.draft, pokud existuje explicitní draft record v režimu edit/empty. Přidat guard: prázdný trim → nabídnout potvrzení / neodesílat.
- **🟡 MED** `state.tsx:553` — Hromadné Hotovo/Archiv/Koš vyvolá N naskládaných undo toastů; každý Zpět vrátí jen jednu položku [undo]
  - Scénář: Vyber 6 vláken a klikni Hotovo (nebo Archiv/Koš). bulkAct v cyklu volá rowAct(id, "done"), který pro každé vlákno vyvolá showToast s vlastní closure „Zpět". Protože showToast je singleton (jeden event-driven toast v ActionToast, každý nový event přepíše předchozí stav), NEuvidíš 6 naskládaných toastů, ale jediný toast s „Zpět", který vrátí jen POSLEDNÍ vlákno; zbývajících 5 změn nemá žádné undo. Navíc hromadné „Odložit" (MailList.tsx:1701–1712) volá setOv přímo bez akce „Zpět", takže je zcela neodvolatelné, přestože řádkové odložení (rowAct case „snooze") undo má — nekonzistence.
  - Oprava: Pro bulk připravit jeden souhrnný snapshot a jeden toast „N zpracováno · Zpět", který obnoví všechna dotčená vlákna najednou (a přidat undo i hromadnému snooze).
- **🟡 MED** `state.tsx:858` — quickTask: rychlý dvojklik založí dva stejné úkoly (chybí pojistka proti dvojímu odeslání) [state]
  - Scénář: Uživatel dvakrát rychle klikne na ikonu „Email → úkol" (RowActs, MailList.tsx:284-285; totéž MailThread.tsx a PeekPanel) nad stejným vláknem dřív, než se dokončí PowerSync roundtrip a re-render. Oba kliky projdou guardem na state.tsx:858 (taskLinks[id] je pořád prázdné kvůli stale closure), každý zavolá bridge.onCreateTask s vlastním crypto.randomUUID() → vzniknou dva identické úkoly „Odpovědět: {subj}" v osobní Schránce navázané na tentýž mail_th. Chip NAVÁZANÉ ÚKOLY ukáže 2 kopie. Dopad je nízký a snadno napravitelný (uživatel jeden úkol smaže), bez ztráty dat či bezpečnostního rizika — proto medium, ne high. Oprava: synchronní in-flight zámek (useRef Set id nastavený hned před voláním), nebo before-INSERT `SELECT 1 FROM tasks WHERE mail_th=?`.
  - Oprava: Přidat synchronní in-flight zámek (useRef Set id, který se hned nastaví a uvolní až po dokončení onCreateTask), nebo v bridge.onCreateTask před INSERT ověřit `SELECT 1 FROM tasks WHERE mail_th=?`.
- **⚪ LOW** `state.tsx:553` — bulkAct done/arch/trash vysype N toastů (jeden na položku) místo souhrnu [correctness]
  - Scénář: Vyber 8 vláken a klikni na hromadnou akci Archivovat (nebo Hotovo/Koš). Protože toast je singleton (lib/toast.tsx: jeden useState, každý showToast přepíše předchozí), NEobjeví se 8 toastů, ale jen JEDEN — od naposledy zpracované položky. Jeho „Zpět" ale volá setOv jen pro to jedno poslední id (undoPatch v rowAct), takže vrátí pouze jedno vlákno a zbývajících 7 zůstane archivovaných — hromadné undo chybí. Ve smíšeném/osobním výběru navíc done→arch (ř. 506) způsobí, že toast hlásí „Archivováno" i pro akci Hotovo. Oprava dle návrhu (sbírat patche bez toastu a ukázat jediný souhrnný toast s hromadným undo, jako u větve „unread") řeší obojí.
  - Oprava: V bulkAct sbírat patche bez showToast (zavést variantu rowAct bez toastu) a na konci ukázat jediný souhrnný toast s hromadným undo, obdobně jako u „unread".
- **⚪ LOW** `state.tsx:654` — Druhé odeslání do 10 s tiše nahradí undo prvního — první už nelze vzít zpět [undo]
  - Scénář: doSend při každém odeslání bezpodmínečně přepíše jediný `prevSend.current`, zruší jediný `undoTimer` a restartuje jediný `undo` stav. Odešli vlákno A (běží 10s okno „Zpět"), do 10 s přepni na vlákno B ve sdíleném workspace a odešli ho → prevSend nyní ukazuje na B, timer i lišta A jsou nahrazeny. undoBack obnoví jen B; odeslání A je nenávratně potvrzeno a jeho undo lišta tiše zmizela bez varování. Nízká závažnost: jde o prototyp/demo mailu (odeslání je simulované) a vyžaduje dvě odeslání během 10s okna — reálný, ale okrajový a řídký případ.
  - Oprava: Držet frontu/zásobník posledních odeslání s per-vlákno undo, nebo alespoň před přepsáním prevSend commitnout předchozí a uživatele upozornit, že starší odeslání už nelze vrátit.

## apps/web/src/components/Calendar.tsx  (5)

- **🔴 HIGH** `Calendar.tsx:148` — toggleTask volán bez actorId → shared_all úkol dokončen za všechny (obchází R2) [invariant]
  - Scénář: Netýká-opakující se úkol v režimu shared_all (2+ přiřazených, „každý zvlášť") s vyplněným due_date se zobrazí v Kalendáři. Uživatel A klikne na kruhový checkbox (CalCheck, Calendar.tsx:148, resp. CalendarMonth.tsx:246, nebo podúkol v TaskDetailPanel.tsx:1334). toggleTask je voláno bez actorId → podmínka shared_all na tasks.ts:135 je false → propadne do obecné větve (ř. 264–281), která přímo nastaví tasks.completed_at a status. assignments.completed_at všech přiřazených zůstanou null. Úkol se tak jeví dokončený všem (i uživateli B, který nic neudělal), přestože R2 vyžaduje dokončení odvozené až po dokončení VŠECH účastí. Per-osoba stav je rozjetý oproti stavu úkolu.
  - Oprava: Předat přihlášeného uživatele: toggleTask(tk, session?.user?.id) ve všech callerech (Calendar.tsx:148, CalendarMonth.tsx:246, TaskDetailPanel.tsx:1334). Zvážit učinit actorId povinným parametrem, aby TS odhalil chybějící případy.
- **⚪ LOW** `Calendar.tsx:2346` — PlanningPanel „Přeplánovat" posouvá due_date hromadně bez R4 dotazu a bez undo — u opakovaného úkolu přesune kotvu celé řady [undo]
  - Scénář: Uživatel má v postranním panelu Plánování zpožděné úkoly a klikne „Přeplánovat". reschedule() (Calendar.tsx:2346-2353) přepíše due_date všech overdue úkolů na dnešek surovým UPDATE bez pushUndo. Na rozdíl od moveTask (ř.478) a ostatních zápisů tasks nelze akci vrátit přes ⌘Z — původní termíny se přepíšou bez možnosti obnovení historií undo (uživatel je musí ručně přetáhnout/nastavit zpět). Žádná trvalá ztráta ani porušení R4 (posun projekce opakování je stejné chování jako drag-drop moveTask a je záměrně odloženo). Oprava: obalit hromadný přepis do jednoho pushUndo (undo vrátí předchozí due_date všech dotčených), ideálně v jedné transakci; R4 dialog není potřeba, protože kalendář jej záměrně nemá ani u moveTask.
  - Oprava: Přeplánování směrovat přes lib/tasks.ts (respekt R4 pro opakované) a obalit `pushUndo`, ideálně jednou transakcí; u opakovaných se ptát na rozsah řady.
- **⚪ LOW** `Calendar.tsx:2146` — Resize úchyty bloků jsou vysoké 5 px a checkboxy 11–13 px → na dotyku/mobilu nesplnitelné cíle (<24 px), resize kolizně spouští move [mobile]
  - Scénář: On a phone/tablet the user opens the calendar in Day or Week view (TimeGrid, rendered regardless of isMobile). Resizing a timed block requires hitting a 5px-tall handle (Calendar.tsx:2154/2168) at the block's top/bottom edge; with touchAction:none and finger-sized contact, the tap almost always lands on the block body and starts a move instead of a resize. There is no event collision — blockDown stopPropagation (1162) means resize, when actually hit, fires correctly. Likewise the round CalCheck complete-checkbox is 12-13px (2193, 1002, 1663, 1770) with no hit-slop, below the ~24-44px touch-target guidance, so it is easy to mis-tap. Note the finding's "11px month view" detail is inaccurate (that line range is the rootH effect; smallest checkbox is 12px). Both actions have alternative paths in the task detail modal, so this is an accessibility/usability friction rather than a blocking failure. Suggested fix stands: add an invisible ~16px+ hit-slop to the resize handles and enlarge the checkbox hit area to >=24px.
  - Oprava: Zvětšit dotykovou plochu úchytů (např. neviditelný hit-slop ~16px) a checkboxů na >=24px, nebo resize na dotyku nahradit explicitním gestem.
- **⚪ LOW** `Calendar.tsx:399` — Globální keydown efekt má deps jen [mode]; goToday zachytává zastaralé PPM (hustota) → „d" po změně hustoty scrolluje na špatnou pozici [state]
  - Scénář: V day/week režimu uživatel v gear menu přepne hustotu (comfortable↔spacious), čímž se změní PPM, ale ne mode. Keydown efekt (deps [mode]) se znovu nenaváže, takže handler drží zastaralou closure goToday se starým PPM. Po stisku „d" se scrollTop spočítá jako nowMin*staréPPM-90, zatímco layout je už škálovaný novým PPM → scroll skočí na špatnou minutu (v poledne ~230px vedle). Pozn.: shiftCur zastaralé NENÍ (closuruje jen mode v deps a stabilní setCur); jediná zastaralá hodnota je PPM v goToday. Oprava: číst PPM přes ref, nebo přidat PPM/density do deps.
  - Oprava: Přidat do deps `density`/`PPM` (a ostatní použité hodnoty), nebo číst PPM přes ref, aby handler pracoval s aktuální hustotou.
- **⚪ LOW** `Calendar.tsx:1460` — Popover přetečených celodenních úkolů se nezavírá klávesou Esc a jeho pozice není clampnutá zleva/zdola [overlay-esc]
  - Scénář: Nejsilnější reprodukovatelná cesta: v měsíčním zobrazení má buňka dne ve spodní řadě víc celodenních úkolů, než se vejde → zobrazí se chip „+N more". Kliknutím se otevře adPopover s top = r.bottom+4 (blízko spodní hrany okna). Popover s maxHeight 320 přeteče pod spodní hranu viewportu a odřízne spodní úkoly i tlačítko „otevřít den"; není clamp topu k window.innerHeight. Zároveň Esc popover nezavře (globální keydown Escape neřeší, popover vlastní handler nemá) — na rozdíl od očekávané konzistence overlayů se musí zavřít klikem/tapem/tlačítkem. Poznámka: záporný left je jen při window.innerWidth < 280 px, prakticky nedosažitelné.
  - Oprava: Přidat keydown Esc → setAdPopover(null) a clampnout left na >=8 a top tak, aby maxHeight nepřetekl pod spodní hranu okna.

## apps/web/src/lib/chainAdvance.ts  (5)

- **🔴 HIGH** `chainAdvance.ts:112` — Postup: advance/cascadePull/reflow mění chain_steps a cizí due_date bez undo záznamu [undo]
  - Scénář: Postup v režimu Řetězec, jehož NÁSLEDUJÍCÍ krok má due_date v minulosti (po termínu). Uživatel odškrtne aktivní krok → toggleTask (tasks.ts:281) pushne undo jen pro completed_at/status_id zdrojového úkolu, pak advanceChainForTask aktivuje další krok a cascadePull (chainAdvance.ts:112) přepíše due_date navazujícího úkolu na dnešek + reflowChain posune termíny všech dalších kroků — bez pushUndo. Uživatel dá ⌘Z: undo obnoví jen completed_at zdrojového úkolu (nevolá rewind). Následující krok zůstane aktivovaný a posunuté due_date řetězce se nevrátí. Otevření detailu spustí repairChain, které narovná step_state (zdrojový krok zpět active, další dormant), ale posunuté due_date NEnarovná (repairChain úkoly nemění). Termíny řetězce tak zůstanou trvale posunuté a propíšou se přes sync i kolegům. Pozn.: pokud navazující krok NENÍ po termínu, cascadePull/reflow se neprovede a undo+repairChain plně zkonvergují — trvalá škoda je vázaná na overdue navazující krok.
  - Oprava: Buď advance/rewind zabalit do jednoho undo záznamu spolu s toggle (snapshot dotčených chain_steps.step_state/activated_at + cizích tasks.due_date před změnou a re-apply v undo), nebo cascadePull/reflow z undo cesty vyloučit a spoléhat výhradně na idempotentní repairChain při otevření detailu.
- **🟡 MED** `chainAdvance.ts:254` — Un-checking a chain-step task from any list silently un-completes all downstream steps and is not undoable [undo]
  - Scénář: Skutečná vada = nekompletní undo (kaskádové od-dokončení je záměr). V postupu (Postup/štafeta) s hotovými pozdějšími kroky uživatel odškrtne dřívější krok (z Boardu/Přehledu/detailu). rewindToStep od-dokončí všechny navazující úkoly (tasks.completed_at=NULL) — to je dle designu. Pak uživatel stiskne Undo: pushUndo (tasks.ts:277-280) obnoví jen completed_at/status tohoto úkolu přes writeDone; downstream od-dokončení ani step_state se neobnoví, protože nebyly zachyceny do undo záznamu. repairChain úkoly nemění, takže navazující úkoly zůstanou nehotové a nelze je přes undo obnovit — vznikne nekonzistentní stav (tento úkol hotový, navazující nehotové). Oprava: advanceChainForTask/rewindToStep má pořídit kompenzační snapshot (downstream completed_at + step_state) a zaregistrovat ho do STEJNÉHO undo záznamu jako toggleTask. Zúžení oproti nálezu: závažnost medium (ne high) — vada je za nikovou funkcí postupů a konkrétní sekvencí (odškrtnutí ne-čelního kroku + Undo).
  - Oprava: Either confirm before a rewind that touches already-done downstream steps, or make advanceChainForTask capture a compensating snapshot and register it with the same undo entry so undo restores step_state and downstream completed_at too.
- **🟡 MED** `chainAdvance.ts:264` — rewindToStep nuluje tasks.completed_at napřímo → obchází R2 shared_all [invariant]
  - Scénář: Postup obsahuje pozdější krok = úkol v režimu shared_all s ≥2 účastníky (Postupy.tsx:1151-1195). Všichni účastníci krok normálně odškrtnou přes hlavní checkbox → toggleTask (tasks.ts:183-208) nastaví každému assignments.completed_at a odvozeně tasks.completed_at; postup se posune dál. Uživatel pak u dřívějšího kroku klikne „Vrátit sem" (Postupy.tsx:999 → rewindToStep). rewindToStep projde všechny kroky od cíle dál a u shared_all úkolu spustí UPDATE tasks SET completed_at = NULL (chainAdvance.ts:264-267), ale per-osoba assignments.completed_at nechá nastavené. Výsledek: úkol se jeví jako nedokončený (tasks.completed_at NULL, step dormant), přesto mají všichni účastníci completed_at → porušený odvozený invariant R2; navíc bez undo. repairChain drift nenapraví. Oprava: v rewind cestě u shared_all vynulovat v téže transakci i všechny assignments daného úkolu (příp. řešit přes toggleTask/pomocnou funkci srovnávající assignments), obdobně opravit i přímý zápis v completeStep (Postupy.tsx:676), který navíc obchází i R9 dopočet status_id.
  - Oprava: V rewind/complete cestě řešit shared_all přes toggleTask/pomocnou funkci, která zároveň srovná assignments.completed_at, ne přímý UPDATE tasks. Případně u shared_all vynulovat i všechny assignments daného úkolu v téže transakci.
- **⚪ LOW** `chainAdvance.ts:167` — gate='manual' steps auto-activate — the builder's 'manual' template steps behave identically to after_previous [correctness]
  - Scénář: Create a flow from the "Žádost o grant" template (Postupy.tsx:180-222), whose 4th step "Odeslat žádost" has gate:'manual' — implying a human hold before the grant application is sent. Complete steps 1-3. On completing step 3, advanceChainForTask (chainAdvance.ts:162-176) finds step 4 dormant with all priors closed and calls activateRun, activating it immediately regardless of its 'manual' gate. The step never waits for manual activation; activateStepManually (line 241) that would honor the gate is dead code, never invoked. Thus the 'manual' gate stored in the DB and shipped in templates has no runtime effect and contradicts the module docstring — dead, misleading semantics.
  - Oprava: Either honor gate='manual' (require activateStepManually and stop auto-advance at a manual step) or drop manual from templates and the schema to avoid implying a behavior that doesn't exist.
- **⚪ LOW** `chainAdvance.ts:171` — handoffName volá síť (fetch members) v toggle cestě a zobrazí toast 'Předáno → X' i offline [correctness]
  - Scénář: Offline (výchozí offline-first režim) uživatel odškrtne krok postupu (Postupy) → advanceChainForTask aktivuje další dormant krok a na ř. 171 spustí fire-and-forget `void handoffName(st.task_id).then(showToast)`. handoffName přečte assignments.user_id lokálně, ale k překladu na jméno volá fetch /api/projects/{pid}/members; offline fetch selže → catch → fallback „kdokoli z týmu". Toast ukáže „Předáno → kdokoli z týmu" i když je krok přiřazen konkrétní osobě. Jména členů nejsou v lokální DB (project_members bez name), takže offline je jméno nedostupné a fallback nelze odlišit od skutečně nepřiřazeného kroku → zavádějící text + zbytečný síťový request na pozadí při každém předání. POZN.: volání NEblokuje toggle (není awaited), takže rámování „kritická toggle cesta" je nepřesné; a lokální oprava by musela sáhnout do react-query cache, ne do (neexistující) lokální tabulky jmen.
  - Oprava: Jméno přiřazeného řešit z lokálních dat (assignments.user_id + lokální cache členů / react-query cache) místo synchronního fetch v toggle cestě; síť jen jako doplněk.

## apps/web/src/components/AddTaskModal.tsx  (4)

- **🔴 HIGH** `AddTaskModal.tsx:723` — AddTaskModal nemá in-flight guard → dvojklik/dvojí Enter vytvoří dva úkoly [state]
  - Scénář: Uživatel vyplní validní úkol a rychle dvakrát klikne na „Přidat" (nebo dvakrát/přidržením stiskne Enter/⌘Enter) dřív, než první `await powerSync.execute` doběhne a zavolá onClose(). Oba synchronní klik-eventy projdou guardem na ř. 725 (disabled se mezi nimi nezmění) → vloží se DVA úkoly, dvě sady assignments. Při připojení do postupu (flowAttach) obě souběžná volání přečtou stejný stav chain_steps a vloží dva kroky na stejnou position (maxPos+1), čímž poškodí pořadí štafety. Duplicitní data se přes PowerSync rozšíří celému týmu.
  - Oprava: Přidat useRef/useState `submitting` flag: na začátku submit `if (submitting.current) return; submitting.current = true;` a v finally uvolnit; zároveň disablovat tlačítko během běhu.
- **🟡 MED** `AddTaskModal.tsx:761` — Barva zvolená při vytvoření se ukládá do tasks.color (sdílená), obchází per-user R6 [invariant]
  - Scénář: Uživatel A založí úkol a v popoveru „Barva" zvolí např. červenou → draft.color=červená se v submit() (AddTaskModal.tsx:761) uloží do sdíleného sloupce tasks.color, který se synchronizuje celému týmu. V seznamu TaskItem (ř. 194) renderuje meta.color ?? task.color; protože do task_user_colors se nic nezapsalo, meta.color je prázdné a všem uživatelům se zobrazí červená z task.color (poruší R6 per-user barvu). Když A (nebo kdokoli) otevře detail úkolu, userColor se čte jen z task_user_colors (TaskDetailPanel.tsx:221) a je null → žádný barevný vzorek není označen jako vybraný (ř. 1048), přestože úkol v seznamu červený je. Oprava: po insertu úkolu zapsat zvolenou barvu do task_user_colors pro aktuálního uživatele (konzistentně s detailem přes setUserColor), nebo tasks.color pro barvu vůbec nepoužívat.
  - Oprava: Barvu z AddTaskModal zapisovat po insertu do task_user_colors pro aktuálního uživatele místo tasks.color, konzistentně s detailem; nebo tasks.color zcela opustit.
- **🟡 MED** `AddTaskModal.tsx:368` — Esc v AddTaskModal zahodí celý rozepsaný draft místo zavření jen popoveru [overlay-esc]
  - Scénář: Uživatel otevře AddTaskModal, rozepíše úkol a nastaví pole, pak otevře některý field popover (draft.pop !== "") — např. „termín" nebo „projekt". Fokus je v inputu popoveru (textové vyhledávání projektu, číselné inputy dní/trvání/počtu, případně date/time). Stiskne Esc s očekáváním, že se zavře jen popover (dle komentáře ř. 365-366). Místo toho okenní handler (ř. 372) zavolá onClose() a zavře CELÝ modal — rozepsaný úkol i všechna nastavená pole jsou nenávratně ztracena (draft je jen v useState). Chyba nastane i při fokusu v titulním textarea, pokud je popover otevřený a našeptávač není aktivní, protože onNameKey pro Escape v tom případě nic neprovede a událost probublá na window.
  - Oprava: V handleru nejdřív: pokud draft.pop !== "" → patch({pop:""}) a return; teprve při zavřeném popoveru onClose(). Případně e.stopPropagation z inputů popoveru.
- **⚪ LOW** `AddTaskModal.tsx:782` — Přiložené soubory se při odeslání tiše zahodí [data-loss]
  - Scénář: Uživatel otevře popover „Příloha" v AddTaskModalu, přes file input vybere soubory (uloží se jen jejich názvy do draft.attached, ř. 1782-1787), chip zobrazí počet „N×" (ř. 649-652). Po odeslání submit() (ř. 723-810) vloží úkol do tabulky tasks a přiřazení do assignments, ale draft.attached ignoruje — žádný zápis do tabulky attachments (v klientu ani neexistuje INSERT ani sync). Přílohy se tiše zahodí, přestože uživatel viděl potvrzený počet. Pozn.: obsah souborů se stejně nikdy nečte, jde tedy o neimplementovanou funkci s zavádějícím UI, ne o ztrátu reálně nahraných dat.
  - Oprava: Buď implementovat upload/persist příloh v submit, nebo (dokud není hotové) přílohový chip skrýt/označit jako demo, ať uživatel neočekává uložení.

## apps/web/src/screens/Postupy.tsx  (3)

- **🔴 HIGH** `Postupy.tsx:638` — FlowDetail Esc/Enter listener fires under stacked task-detail overlay [overlay-esc]
  - Scénář: Open a Postup detail (z-index 43). Click a step name → openTask() renders TaskDetailPanel on top (z-[70]/[71]). FlowDetail's window keydown handler is still mounted with NO topmost-layer guard. (a) Press Enter while focus is on a button (not an input) → line 651-654 runs completeStep(now) and silently completes the Postup's active step even though the user is reading the task detail. (b) Press Esc → FlowDetail.onClose() closes the flow underneath the task detail. TaskDetailPanel guards its own Esc with document.querySelector('[data-esc-layer]') (line 186), but FlowDetail neither sets that marker nor checks it, so the coordination is one-way and broken.
  - Oprava: Give FlowDetail the same data-esc-layer coordination: mark the panel with data-esc-layer and, in its keydown handler, bail out (both for Esc and Enter) when a higher layer such as the task detail is open (e.g. when useTaskDetail().openId is set or a higher data-esc-layer exists).
- **🟡 MED** `Postupy.tsx:1113` — Clearing the anchor date in the flow builder writes NaN due dates to every step [correctness]
  - Scénář: The anchor <input type='date'> (line 1282-1288) can be cleared to '', and create() only validates name/rows/projectId (line 1117), never the anchor. With anchor='', effAnchor='' flows into addDays('', r.offset): new Date('T00:00:00') is Invalid Date, so getFullYear() is NaN and every step's due_date becomes 'NaN-NaN-NaN' (line 30-34 / 1139). chains.anchor_date is also stored as '', so any later reflow (toggleChainWeekend etc.) keeps producing invalid dates.
  - Oprava: Block create (disable the button / early-return) when anchor is empty or not a valid ISO date, and validate the date input value.
- **🟡 MED** `Postupy.tsx:1081` — Changing the builder project after picking assignees leaves stale who[] → assignments for non-members (R5) [invariant]
  - Scénář: V builderu „Nový postup" je předvybrán projects[0] (projekt A); po přepnutí project <select> se přenačtou jen members, ne rows. Uživatel vybere přiřazené pro projekt A, pak přepne select na projekt B. Avatarová řada ukáže členy B (staré A picky se nevykreslí → vypadají vyčištěné, a nejdou odškrtnout, protože jejich tlačítka chybí). r.who ale stále drží A user id. create() (ř. 1190-1194) vloží do assignments A user id s project_id = projekt B (stepProject = r.project || projectId), čímž přiřadí lidi, kteří nejsou členy projektu B — porušení R5 (row-level přístup). I případný re-pick B členů A id neodstraní (ř. 1469 jen appenduje).
  - Oprava: Reset each row's who[] (and role) when projectId changes, or filter who[] to the current project's member ids before inserting assignments.

## apps/web/src/components/TaskItem.tsx  (3)

- **🔴 HIGH** `TaskItem.tsx:249` — Rychlá plánovací pilulka na kartě obchází R4 blok opakovaných úkolů [invariant]
  - Scénář: Base řádek opakovaného úkolu (task.recurrence nastaveno, plain id bez @) se zobrazí v seznamu jako TaskItem. Uživatel najede myší na řádek — objeví se rychlá plánovací pilulka (Dnes/Zítra/Příští týden), protože selectable=true → sched je předáno a TaskCard ji na group-hover ukáže. Klik na tlačítko spustí sched.onShift (TaskItem ř. 249-256), které provede přímý UPDATE tasks SET due_date bez kontroly task.recurrence. Tím se tiše posune kotva due_date celé opakovací řady, aniž by se uživatele zeptalo tento/tento a další/celá řada (R4). Přitom swipe na tomtéž řádku (reschedule, ř. 75-78) i BulkBar opakované úkoly blokují toastem recurringBlocked/recurringSkipped — nekonzistence je jistá.
  - Oprava: Do `onShift` přidat stejnou pojistku jako swipe/BulkBar: `if (task.recurrence_rule) { showToast(t('qsched.recurringBlocked')); return; }` (pozn.: použij recurrence_rule, ne recurrence — viz další nález).
- **🟡 MED** `TaskItem.tsx:249` — Rychlé přeplánování (sched.onShift) obchází R4 ochranu opakovaných úkolů [correctness]
  - Scénář: U opakovaného úkolu (recurrence_rule) najedu myší na řádek — objeví se hover tlačítka rychlého přeplánování (data-qsched: Dnes/Zítra/Příští týden). Kliknu na některé → `sched.onShift` provede přímý `UPDATE tasks SET due_date` bez kontroly opakování, čímž posune kotvu celé opakovací řady, aniž by proběhl dialog „tento / tento a další / celá řada". Swipe i BulkBar tuto akci u opakovaných úkolů blokují (qsched.recurringBlocked / bulk.recurringSkipped), takže uživatel má ochranu R4 nekonzistentní podle zvolené cesty. Závažnost snížena z high na medium: mutace se týká jediného sloupce (due_date), zapisuje undo záznam přes pushColumnUndo (je tedy vratná) a nic dalšího nekaskáduje — nejde o destruktivní ani nevratnou operaci, ale o porušení invariantu R4 a nekonzistenci UI.
  - Oprava: Do `sched.onShift` přidat stejnou pojistku jako v `reschedule`: `if (task.recurrence || task.recurrence_rule) { showToast(t('qsched.recurringBlocked')); return; }` před UPDATE. Pozn.: swipe guard testuje `task.recurrence` (label), engine používá `recurrence_rule` — sjednotit na `recurrence_rule`, aby guard nezávisel na tom, zda je vyplněný lidský label.
- **⚪ LOW** `TaskItem.tsx:129` — Swiping a completed task left shows an empty gray reveal with no label or action [empty-state]
  - Scénář: For a done task leftActs is [] (line 98). dragAct = sw.dx>0 ? rightActs[0] : (sw.mag==='l2'?leftActs[1]:leftActs[0]) → undefined on any left drag. The reveal background (line 142, `sw.dx !== 0`) still renders a growing gray panel-2 pill, but the inner `{dragAct && …}` label is skipped, and on release onSwipe returns early for doneTask. Result: dragging a completed row left produces a meaningless empty gray swipe animation that does nothing, unlike every other swipe which shows a colored labeled action.
  - Oprava: When doneTask (no left actions), clamp/disable left drag in useSwipe for this row or suppress the reveal background when dragAct is undefined so no empty pill is drawn.

## apps/web/src/lib/quickadd/parse.ts  (2)

- **🔴 HIGH** `parse.ts:130` — "za N dní/dny" relative date is misparsed as a multi-day span and mangles the name [correctness]
  - Scénář: Do QuickAdd zadej "zavolat za 3 dny". Pravidlo 5 (vícedenní, parse.ts:130) zachytí "3 dny" → draft.days=3 a vyřízne "3 dny". Neexistuje žádné pravidlo pro relativní datum "za N dní", pravidlo 6 tedy due nenastaví a recurrence vrací null. Předložka "za" se nikdy nevyřízne. Výsledek: úkol s názvem "zavolat za", days=3 (3denní úkol) a BEZ termínu, místo úkolu s termínem za 3 dny. Oprava: přidat pravidlo relativního data "za N dn[íiy]" → due = addDays(today, N), které poběží před pravidlem 5, nebo nespouštět vícedenní pravidlo, pokud mu předchází "za".
  - Oprava: Add a relative-date rule "za N dn[íiy]" → due = addDays(today, N) that runs before the multi-day rule, or make the multi-day rule not fire when preceded by "za".
- **🟡 MED** `parse.ts:91` — Duration regex "min\p{L}*" over-matches any word starting with "min" preceded by a number [correctness]
  - Scénář: User types a task like "koupit 5 minerálek" (buy 5 mineral waters) in QuickAdd. The duration rule at parse.ts:91 matches "5 minerálek" via \p{L}*, sets durationMin=5, and cuts the span, so the created task is named just "koupit" with a spurious 5-minute duration. Same class: "3 minipočítače" (dur=3), "2 ministři" (dur=2), "10 minulých" (dur=10) — any number immediately followed by a word starting with "min". The live highlight preview shows the (wrong) duration span, so an attentive user can catch it, but pressing enter directly commits the corrupted name+duration.
  - Oprava: Anchor the unit to a word boundary: require "min" to be followed by end-of-token or a duration suffix, e.g. /(\d+)\s*min(?:ut\p{L}*)?(?![\p{L}])/ (accept only "min"/"minut..."), so "minerálek" no longer matches.

## apps/web/src/components/WatsonPanel.tsx  (2)

- **🔴 HIGH** `WatsonPanel.tsx:129` — Watson drawer nenese data-esc-layer → stejný únik klávesové navigace seznamu (mazání/priorita) pod otevřeným panelem [keyboard]
  - Scénář: Na Dnes/Úkoly(list)/Nadcházející(list) otevři Watson drawer (pill v headeru / spodní lišta). Fokus padne na zavírací tlačítko (button, ne input) → typing=false; panel nenese data-esc-layer ani nenastavuje openId. useKbNav podkladové obrazovky dál běží: stiskem j/k lze vybrat řádek za panelem, poté Backspace/Delete zavolá deleteTaskWithUndo(cur) a smaže úkol schovaný za drawerem, klávesy 1-4 mu přepíší prioritu. Oprava: doplnit data-esc-layer na kořenový dialog (WatsonPanel.tsx:129), shodně s CommandPalette/AddTaskModal/TaskModal.
  - Oprava: Doplnit data-esc-layer na kořenový dialog Watson panelu (ř. 129), aby ho kbNav.tsx/BulkBar/TaskDetail rozpoznaly jako otevřenou vrstvu.
- **🟡 MED** `WatsonPanel.tsx:36` — Esc ve WatsonPanelu zavře i vrstvu nad ním (dvojité zavření) [overlay-esc]
  - Scénář: On any non-/mail screen: open the Watson drawer (header pill/bell), press ⌘K to open the CommandPalette over it, then press Esc once. Both the palette and the Watson drawer close simultaneously, instead of Esc closing only the top palette and leaving Watson open. Fix: add `if (document.querySelector("[data-esc-layer]")) return;` before onClose() in WatsonPanel.tsx:36, matching the sibling-overlay pattern.
  - Oprava: Před `onClose()` přidat `if (document.querySelector('[data-esc-layer]')) return;` (vzor MailScreen/PeekPanel/BulkBar).

## apps/web/src/mail/SearchOverlay.tsx  (2)

- **🔴 HIGH** `SearchOverlay.tsx:80` — Osobní (šifrovaná) schránka je prohledávatelná i s texty zpráv, přestože UI tvrdí opak [correctness]
  - Scénář: V ⌘K hledání zadej text z osobního mailu (např. „koláče" nebo „chatě" ze seed vlákna „tata"/„alza") → osobní vlákno se objeví ve výsledcích včetně snippetu; případně zadej operátor `schranka:osobni`, který vrátí VÝHRADNĚ osobní vlákna. Přitom prázdný stav (ř. 271) slibuje „Osobní schránka je mimo týmové hledání." Filtr results (ř. 80–96) nemá `if (t.personal) return false;` a bodyOf() prohledává i těla osobních vláken. Pozn.: modul je demo se seed daty, takže reálně šifrovaný obsah zatím neexistuje — jde o porušení zobrazeného kontraktu a design invariantu, ne o únik produkčně šifrovaných dat.
  - Oprava: V filtru results přidat `if (t.personal) return false;` (případně povolit jen když je aktivní folder osobni), aby fulltext ani operátory nikdy nesáhly do osobních vláken.
- **⚪ LOW** `SearchOverlay.tsx:91` — Operátor is:neprectene ignoruje per-osoba nepřečtenost [correctness]
  - Scénář: V per režimu: otevři přečtené vlákno (seed t.unread=false), pak „Označit jako nepřečtené" → ov.read=false. Seznam i sidebar badge ho počítají jako nepřečtené (unreadFor=true), ale is:neprectene ho vynechá, protože ř. 91 čte t.unread(false) místo overridu. Opačný směr v shared režimu: seed-unread vlákno s neprázdným readBy je pro seznam/badge přečtené, ale is:neprectene ho stále vrátí (ř. 91 ignoruje readBy). is:neprectene tedy vrací jinou množinu než seznam a badge. Oprava: použít m.unreadFor(t).
  - Oprava: Použít `m.unreadFor(t)` konzistentně jako ve zbytku modulu.

## apps/web/src/mail/MailThread.tsx  (9)

- **🟡 MED** `MailThread.tsx:984` — Banner „AI je pro granty@ vypnutá“ je natvrdo — ukáže špatnou schránku [correctness]
  - Scénář: Správce v AdminScreen (řádek dané schránky, tlačítko „off") vypne AI např. pro info@ → setAi('info','off') → m.setAdmAi('info','off'). Uživatel otevře vlákno z info@ (t.mb==='info', !t.personal): aiOffOn===true. Titul badge „AI vypnuta" i tělo pod hlavičkou ukážou natvrdo „AI je pro granty@ vypnutá (osobní údaje žadatelů)…", tedy jmenují cizí schránku. Oprava: skládat text z mb?.short / mb?.addr (v MailThread.tsx je mb=MB[t.mb] dostupné) a odstranit granty-specifickou vsuvku.
  - Oprava: Skládat text z mb?.short / mb?.addr místo natvrdo „granty@“.
- **🟡 MED** `MailThread.tsx:3652` — Zvolený podpis (SigBlock) se zobrazí pod editorem, ale doSend ho k odeslané zprávě nepřidá [correctness]
  - Scénář: V otevřeném vlákně otevřu composer odpovědi, v toolbaru přes SigPicker (MailThread ř. 3409) vyberu podpis. Composer vykreslí SigBlock pod editorem (ř. 3652) a UI/dokumentace (SigPicker.tsx ř. 5–6, patička popoveru) slibují „při odeslání se k němu přidá". Odešlu (checkSend → state.tsx doSend, ř. 620–668). doSend ale sestaví tělo jen z draft textu (ř. 622, 635) a přidá SentMsg do sentX; sigBody se nikdy nepřipojí. Odeslaná zpráva se vykreslí do vlákna (msgsAll ř. 422, render ř. 2096) BEZ podpisu — přestože composer podpis ukazoval. Vzniká viditelný nesoulad mezi tím, co UI slibovalo, a tím, co je ve vlákně.
  - Oprava: V doSend připojit sigBody(m, mb) k tělu zprávy (nebo aspoň sjednotit slib v UI/komentáři s realitou demo modulu).
- **⚪ LOW** `MailThread.tsx:2514` — Přeposlat bere jen poslední zprávu a hlavičku „Od:“ vždy připíše externímu odesílateli [correctness]
  - Scénář: Ve vlákně, kde poslední zpráva je má odchozí odpověď (např. po kliknutí Odpovědět a odeslání, nebo u seed vlákna končícího dir:'out'), kliknu Přeposlat. Sestaví se tělo s hlavičkou `Od: ${t.from.n} <${t.from.addr}>` (vždy původní externí odesílatel) a tělem msgsAll[last].body (můj text). Přeposlaná zpráva tak nesprávně připisuje můj text externímu odesílateli; přeposílá se jen poslední zpráva, ne celé vlákno. Snížení závažnosti na low: jde o demo/mock mail modul bez reálného odeslání, text se předvyplní do okna Nové zprávy, které uživatel před (simulovaným) odesláním vidí a edituje, a nesoulad nastává jen v užším případě přeposílání ihned po vlastní odpovědi.
  - Oprava: Odvodit jméno/adresu z konkrétní přeposílané zprávy (dir out → moje identita/schránka, in → t.from), případně přeposílat celé vlákno nebo umožnit výběr zprávy.
- **⚪ LOW** `MailThread.tsx:644` — Vložení odkazu do RTE bez sanitizace URL — self-injection do contentEditable [correctness]
  - Scénář: V popoveru odkazu (linkOpen) při zbaleném výběru (kurzor bez označeného textu) zadám do URL inputu řetězec s HTML/uvozovkou, např. `"><img src=x onerror=alert(1)>`. lnkApply vloží přes execCommand('insertHTML', ...) neescapovaný řetězec, ten se interpretuje jako HTML a injektuje/rozbije obsah contentEditable editoru (self-XSS). Schéma javascript: se také nefiltruje. Dosah je omezen na vlastní draft uživatele (žádná doložená cross-user render cesta), proto low.
  - Oprava: Escapovat lnkUrl (uvozovky, <, >) před vložením a whitelistovat schéma (http/https/mailto); pro collapsed výběr vkládat přes textový node + setAttribute, ne přes stringový HTML.
- **⚪ LOW** `MailThread.tsx:1530` — Archivovat / Do koše / Blokovat v ⋯ menu nezavřou otevřené vlákno (na rozdíl od Odložit) [state]
  - Scénář: V otevřeném vlákně kliknu v ⋯ menu na Archivovat (1532) / Do koše (1541) / Blokovat odesílatele (1550) / Nahlásit spam (1560). rowAct nastaví arch/trash/spam overlay a zobrazí undo toast, ale thread pane zůstane otevřený na tomtéž (nyní archivovaném/smazaném/blokovaném) vláknu a dál nabízí composer a odpovídání — zatímco Odložit i Set Aside pane zavřou přes m.closeThread(). Jde o čistě UX nekonzistenci, nic se neztrácí (undo dostupné). Oprava: po arch/trash/spam z otevřeného vlákna zavolat m.closeThread() jako u snooze.
  - Oprava: Po arch/trash/spam z otevřeného vlákna zavolat m.closeThread() (jako u snooze), případně zobrazit prázdný stav.
- **⚪ LOW** `MailThread.tsx:325` — ⌘F handler ve vlákně je aktivní i pod modály (TaskModal/warn/HostPreview) a při psaní do formulářů [keyboard]
  - Scénář: Otevřené vlákno (tid nastaveno) → uživatel otevře TaskModal (Email→úkol) přes tlačítko; modal má z-index 80, vlákenský ⌘F handler zůstává registrovaný na document. Stisk ⌘F (i s fokusem v inputu formuláře) → preventDefault + m.setSoTh(tid) + dispatch watson-mail:search → MailScreen otevře SearchOverlay se z-indexem 75, tedy schované POD modálem. U HostPreview (z 81) je overlay pohřbený ještě hlouběji. Náprava: v handleru ignorovat, když je otevřená vyšší vrstva (taskM/warn/hostPrev), případně když je fokus v editovatelném poli.
  - Oprava: Deaktivovat ⌘F, když je otevřená vyšší vrstva (taskM/warn/hostPrev), nebo ignorovat, když je fokus v editovatelném poli.
- **⚪ LOW** `MailThread.tsx:670` — Detekce zmínky kolegy v interním chatu reaguje na jakékoli „@“ (i e-mailová adresa) [correctness]
  - Scénář: sendChatNow zobrazí toast „Zmíněný kolega dostane upozornění…“ vždy, když text obsahuje '@'. Napíšu do interní poznámky např. „ozvi se na horak@vlnena-sprava.cz“ — text neobsahuje žádnou @zmínku kolegy, přesto se ohlásí, že byl někdo zmíněn a upozorněn. Falešné potvrzení notifikace.
  - Oprava: Detekovat zmínku vzorem @jméno (regex /(^|\s)@\w/) místo prostého includes('@').
- **⚪ LOW** `MailThread.tsx:2013` — Přepínač skrýt/zobrazit AI shrnutí (m.setSum) je globální — ovlivní všechna vlákna [state]
  - Scénář: m.sum je jediný globální bool (default true). Skryji shrnutí ve vlákně A (sum=false) → přepnu na vlákno B přes openThread, který resetuje jen translated, nikoli sum → shrnutí zůstane skryté i v B. Volba přetéká mezi konverzacemi. Kosmetický únik stavu bez ztráty dat, proto low.
  - Oprava: Držet stav skrytí per vlákno (Record<id,bool>), nebo aspoň resetovat v openThread jako u translated.
- **⚪ LOW** `MailThread.tsx:686` — Kolizní pojistka může po vypršení 6 s během otevřeného warn dialogu způsobit falešné znovuvyzbrojení a rušivý toast [state]
  - Scénář: V kolizním vlákně „faktura" (coll:true) s konceptem zmiňujícím přílohu bez připojené přílohy: 1. klik Odeslat → kolizní toast (collArmed=true), return. 2. klik Odeslat (kdykoli, i hned) → projde kolizní pojistkou, ř. 696 OKAMŽITĚ shodí collArmed zpět na false, pak hlídač přílohy otevře warn modal. Potvrdím „Připojit a odeslat" → beginPendSend→efekt→checkSend znovu narazí na `t.coll && !collArmed` (collArmed už je false) → re-arm + matoucí toast „Petra právě dopisuje…" a return. Efekt [pend,m] se vzápětí zopakuje (collArmed=true) a odešle. Klíčová korekce oproti původnímu nálezu: spouštěčem NENÍ vypršení 6s timeru (ten při expiraci dělá jen no-op false→false), ale reset na ř. 696 při druhém kliku; rušivý toast proto blikne při každém potvrzení warn dialogu v kolizním vlákně bez ohledu na čekání. Návrh opravy platí: při potvrzení z warn dialogu volat rovnou doSend (kolize je již vyřešena), případně nechat collArmed „armed" po dobu otevřeného warn modalu.
  - Oprava: Při potvrzení z warn dialogu volat rovnou doSend (kolize už byla vyřešena), nebo pozastavit collTimer, dokud je otevřený warn modal.

## apps/web/src/screens/Cile.tsx  (6)

- **🟡 MED** `Cile.tsx:1559` — Goal delete is immediate and irreversible — no confirmation, no undo [data-loss]
  - Scénář: In GoalDetail, clicking the full-width 'Smazat' (Delete) button in the panel footer (Cile.tsx:1559) immediately calls remove(), which hard-DELETEs all goal_milestones, all goal_projects, and the goal itself (Cile.tsx:1084–1092) with no confirmation dialog and no undo entry. The delete syncs to the server via PowerSync, so the goal and every milestone are lost permanently with no recovery path. This is inconsistent with FlowDetail's two-step rewind confirm (Postupy.tsx) and with task deletion's deleteTaskWithUndo (lib/undo.ts). Corrected: the button is an explicit, clearly-labeled destructive control in a separated footer rather than an inline/adjacent target, so accidental triggering is moderate; the core issue is the absence of any confirm or undo for an irreversible multi-row delete.
  - Oprava: Require a two-step confirm (like the rewind confirm pattern already in FlowDetail) or push a restorable undo entry capturing the goal + milestones + project links before deleting.
- **🟡 MED** `Cile.tsx:576` — Periodic goals never set period_start, so 'per week/month' counts all historical completed tasks [correctness]
  - Scénář: Ve workspace s již dokončenými úkoly odpovídajícími filtru vytvoř nový periodický cíl (periodic != "none") s metrikou count nebo completion. Protože create() nezapíše period_start (zůstane NULL), goalTasks nevyfiltruje historicky dokončené úkoly (ps == null → filtr na ř. 213 přeskočen) a progress se ihned nafoukne (klidně 100 %) na kartě v Cílech i ve widgetu Přehledu (overview.ts:119), dokud uživatel ručně neklikne „Obnovit období". Oprava: nastavit period_start při vytvoření (na now nebo na začátek aktuálního období odvozený z `periodic`), aby první období bylo ohraničené bez ručního resetu.
  - Oprava: Set period_start on creation (to now, or the start of the current period derived from `periodic`) so the first period is bounded without a manual reset.
- **⚪ LOW** `Cile.tsx:1047` — Rapid target +/- clicks lose increments (reads g.target from a stale render instead of functional update) [state]
  - Scénář: On the goal detail stepper, clicking '+' (or '−') faster than the PowerSync watched query re-emits and re-renders GoalDetail causes each rapid click's handler to read the same stale g.target prop and write the same absolute target, so a burst of N clicks advances the target by only one step instead of N. Example: repeatedly clicking '+' to raise a completion target from 90% toward 95% loses some of the +1 increments during the click burst; the displayed value updates on the next re-render so the user can re-click. No data corruption, self-correcting.
  - Oprava: Read the current target from a fresh query/local state or serialize the writes, e.g. compute from the latest DB value inside a transaction rather than from the render-time prop.
- **⚪ LOW** `Cile.tsx:1030` — Goal name autosaves on every keystroke and whitespace-only edits silently diverge from stored value [state]
  - Scénář: User opens a goal detail panel and edits the name. Every keystroke fires a separate UPDATE goals SET name LWW write (an N-char name = ~N sync ops), widening the window in which a concurrent editor's value is clobbered. Separately, if the user clears the field or types only spaces, nameDraft (line 1031) updates to the empty value and the input renders blank, but the `if (v.trim())` guard (line 1032) skips the DB write, so the card at line 376 keeps showing the old persisted name with no feedback that the empty edit was ignored. Closing and reopening the same goal re-initializes nameDraft from g.name, reverting the blank display — so no data is lost or corrupted; the impact is redundant sync traffic plus a transient, self-healing UI inconsistency with missing validation feedback. Fix: commit on blur (or debounce) and give explicit feedback when an empty/whitespace name is rejected instead of silently ignoring it.
  - Oprava: Debounce/commit on blur instead of per-keystroke, and either persist a trimmed empty→revert or block clearing the name with clear feedback rather than silently ignoring it.
- **⚪ LOW** `Cile.tsx:266` — Goal card/detail labels don't re-localize on language switch (view memo missing t dependency) [i18n]
  - Scénář: The `view` useMemo calls goalProgress(..., t) to build localized label/sub strings (line 239-245) but its dependency array (line 266) omits t. Switching app language (cs↔en) while on Cíle leaves every card's progress label/sub (e.g. '3 / 10 hotových') in the previous language until some other dep (goals/tasks/projects) changes and forces a recompute.
  - Oprava: Add t (or the i18n language) to the view memo's dependency list.
- **⚪ LOW** `Cile.tsx:571` — Negative goal target accepted, yielding negative progress % [correctness]
  - Scénář: create() computes tgt = Number.parseInt(target,10) || default (line 571-573). A typed '-5' parses to -5, which is truthy so it is stored as target. goalProgress then divides by a negative target (e.g. count: done/(-5)*100) producing a negative pct; the card value shows a negative percentage and the progress-bar width becomes an invalid negative value. (Note '0' is separately swallowed by || and replaced with the default.)
  - Oprava: Clamp the parsed target to a positive minimum (Math.max(1, ...)) on create, matching adjTarget's own min=1 clamp in the detail view.

## apps/web/src/components/Board.tsx  (5)

- **🟡 MED** `Board.tsx:142` — ⌘Z po přetažení karty nevrátí přeuspořádání (sort_order) ani postup štafety [undo]
  - Scénář: After any board drag, ⌘Z reverts only the dragged task's status_id/completed_at (Board.tsx:142-145). It does NOT revert the column-wide sort_order rewrite (lines 157-164) nor the chain advance (line 166). Most reproducible: a same-column reorder — since done-state is unchanged, the undo's writeStatus is a no-op, so the reordering (the drag's only real effect) is never undone; the 'undone' toast shows but nothing moves. For a cross-column drop that changes done-state, the next chain step also stays activated after ⌘Z (orphaned R4/postup state), though repairChain self-heals step_state when the flow detail is opened (cascadePull due_date/reflow changes persist). Fix: snapshot the affected cards' sort_order into the undo entry and add a reverse chainAdvance step (or run advance outside the undoable block with its own inverse).
  - Oprava: Zabalit do jednoho undo záznamu i snapshot sort_order dotčených karet a reverzní krok chainAdvance (nebo advance provést až mimo undoovatelný blok s vlastním reverzem).
- **🟡 MED** *(uncertain)* `Board.tsx:127` — Drop do sloupce, jehož název úkolův projekt nezná, ztratí status (status_id=null) a přeskočí do prvního sloupce [correctness]
  - Scénář: Board (view=Nástěnka, no project filter) aggregates tasks from all projects via `SELECT * FROM tasks`. IF two projects have non-identical status name sets (e.g. project A has a \"Ke kontrole\" column that project B lacks, or entirely different names), dragging a task from project B onto that A-only column calls resolveStatus(B.project, colKey) which returns null (Board.tsx:102). writeStatus then sets status_id=null (line 140). For a non-done target column newDone stays null, so colKeyOf (lines 93-98) returns firstCol.key: the card visibly jumps to the first column instead of where it was dropped and loses its status. Reachability depends on projects having differing status-name sets, which the codebase neither seeds nor enforces and which the Board comment claims never happens — so the path is plausible but not statically confirmable.
  - Oprava: Buď slučovat sloupce robustněji (mapování ekvivalentních stavů), nebo drop do sloupce, který v projektu úkolu neexistuje, odmítnout (no-op + toast) místo tichého nastavení null a přeskoku.
- **🟡 MED** `Board.tsx:388` — „+ Přidat" v sloupci nezaloží úkol v daném projektu/stavu — zmizí z aktuální nástěnky [correctness]
  - Scénář: Uživatel otevře konkrétní projekt (Úkoly s ?projekt=X), přepne na Nástěnku a v patičce libovolného sloupce klikne „+ Přidat". Board nedostává projektId a volá openAdd() bez prefill → modal se otevře s prázdným draftem, projekt se defaultně nastaví na Schránku (inbox), status_id se při submitu vůbec nezapisuje. Vytvořený úkol tak má project_id = inbox (≠ X) a žádný stav sloupce. Na filtrované nástěnce (scoped na project_id === X) se úkol vůbec neobjeví — spadne do osobní Schránky (díky default due_date=dnes je viditelný v Dnes, ne přímo ztracený). Navíc i na nefiltrované nástěnce by úkol vždy skončil v prvním sloupci, nikdy ve stavu, jehož patička se klikla. Oprava: předat Boardu aktivní projektId a v patičce volat openAdd({ projectId, statusKey }) — což ale vyžaduje i doplnit pole pro stav do AddPrefill/AddTaskModal (dnes chybí).
  - Oprava: Předat Boardu aktivní projektId a v patičce volat openAdd({ projectId, statusKey }) s předvyplněným stavem sloupce; pokud je board multi-projekt, nechat aspoň zvolit projekt, ať úkol nespadne mimo pohled.
- **🟡 MED** `Board.tsx:140` — Board writeStatus zapisuje completed_at napřímo → obchází R2 shared_all [invariant]
  - Scénář: A shared_all task (assignment_mode='shared_all', multiple assignees) is shown on the Board (Ukoly/Nadchazejici/Oblibene screens). A user drags its card into a column whose merged is_done=true. dropTo (Board.tsx:112) computes newDone=new ISO and writeStatus (line 134) runs `UPDATE tasks SET status_id=?, completed_at=? WHERE id=?` for the whole task. assignments.completed_at for all assignees remain null, so tasks.completed_at is set non-derived — the task is completed for everyone instead of only the dragging user's participation, violating R2 shared_all. This diverges from toggleTask, which every other completion surface uses; a subsequent detail/checkbox toggle then reads all-null assignments and can produce further inconsistency. Board additionally bypasses R4 recurrence handling, but the confirmed defect is the shared_all R2 violation.
  - Oprava: V board drop cestě u shared_all úkolů řešit dokončení stejnou logikou jako toggleTask (per-osoba účast + odvozené tasks.completed_at), ne přímým UPDATE completed_at.
- **⚪ LOW** `Board.tsx:132` — Drop do „done" sloupce na již dokončeném úkolu neaktualizuje čas dokončení, ale reordery a undo naznačují změnu [invariant]
  - Scénář: Přetáhnutí úkolu na JINOU pozici v rámci téhož sloupce (platí pro done i nedokončené úkoly, ne jen done): status_id ani completed_at se nemění, takže pushUndo (ř. 142–145) uloží undo/redo identické s aktuálním stavem. Viditelně se změní jen sort_order (ř. 157–164), který ale v undo není. Důsledek: ⌘Z spustí no-op a viditelný reorder nevrátí; navíc fantomový undo krok „spotřebuje" jeden stisk ⌘Z. Neběh advanceChainForTable je korektní (úkol už byl hotový) a zachování completed_at je záměr — to vady nejsou. Oprava: pro čistý reorder (status i completed_at beze změny) pushovat samostatný undo nad sort_order místo no-op status undo.
  - Oprava: Když se status i completed_at nemění a jde jen o reorder, řešit jako čistý reorder (samostatný undo pro sort_order) a nepushovat no-op status undo.

## apps/web/src/screens/Velin.tsx  (5)

- **🟡 MED** `Velin.tsx:356` — Karty firem a řádky (Row) jsou klikací <div> bez klávesové obsluhy [a11y]
  - Scénář: Sdílená komponenta Row (ř. 811-828, `<div onClick>`) i firemní karty (ř. 356-358, `<div onClick>`) reagují jen na myš — nemají `role="button"`, `tabIndex` ani `onKeyDown`. Řádky v kartách Po termínu (ř. 449 `open(r.id)`), Zátěž lidí, Pošta, Cíle v riziku a Vázne v postupech otevírají peek výhradně přes tento klikací div a NEMAJÍ klávesnicově dostupnou alternativu — uživatel na klávesnici/čtečce je nemůže zaměřit ani otevřít (Enter/Space). U přepnutí firmy vada také platí (karta není fokusovatelná), ale zde existuje rovnocenná klávesnicová cesta přes Chip tlačítka (ř. 330-337, reálné `<button>` volající stejný `setFirm`), takže přepnutí firmy z klávesnice funguje. Oprava: převést klikací divy na `<button>` nebo doplnit `role="button"`, `tabIndex={0}` a `onKeyDown` pro Enter/Space u Row i firemních karet.
  - Oprava: Převést klikací divy na <button> (nebo doplnit role="button", tabIndex={0}, onKeyDown pro Enter/Space) u firemních karet i Row.
- **⚪ LOW** `Velin.tsx:283` — Vedení vidí problesknutí zamčené obrazovky při každém načtení [state]
  - Scénář: Vlastník/Admin provede hard refresh (nebo studený start) přímo na routě Velína. App.tsx nejdřív ukáže „…" (session isPending), po dojetí session se namontuje router; workspaces query je ale prázdná (žádná cache, žádné initialData), takže workspaces===undefined → isLeadership=false → na dobu trvání fetch /api/workspaces se ukáže „Velín je jen pro vedení / role Člen". Po dojetí fetch se přepne na dashboard. Jde o momentální kosmetické probleknutí pouze v tomto úzkém případě — NE „při každém otevření" (in-app navigace čte cache okamžitě), NE „při každém refetch/reconnect" (react-query drží předchozí data), a jméno v hlášce NENÍ prázdné (session je gatovaná). Text hlášky je tedy korektní, jen se nesprávně krátce zobrazí oprávněnému uživateli.
  - Oprava: Rozlišit načítání od skutečného odepření: dokud je `workspaces === undefined` (isLoading), zobrazit neutrální skeleton/prázdno, a locked screen ukázat až když data dorazila a leadership je opravdu false.
- **⚪ LOW** `Velin.tsx:424` — KPI „Urgentní vlákna“ je odvozené z top-8 digestu, podhodnocuje a míchá zdroj [correctness]
  - Scénář: Velín odvozuje „Urgentní vlákna" (KPI ř. 424 i karta Pošta) z useMailDigest, který filtruje jen nezavřená ne-osobní inbox vlákna a vylučuje arch/snoozed/muted/trash/spam. NotifCenter (ř. 121–124) počítá p1/p2 přes VŠECHNA vlákna s pouhým !personal && !closed && !sent. Reálně dosažitelná divergence: uživatel urgentní (p1/p2) vlákno odsune (snooze) nebo archivuje → digest ho vypustí, takže KPI/karta ve Velíně ho přestanou počítat, ale zvonek ho dál ukazuje. Velín a zvonek pak zobrazí rozdílný počet urgentních položek pro stejného uživatele; Velín podhodnocuje. (Pozn.: samotné zastropování na 8 z .slice(0,8) je s aktuálními daty nedosažitelné — existují max 2 efektivně urgentní vlákna, k ořezu by bylo potřeba 9+.) Oprava dle návrhu — počítat P1/P2 přes stejnou množinu jako NotifCenter — nekonzistenci odstraní.
  - Oprava: Pro KPI a kartu Pošta počítat P1/P2 přes všechna nezavřená týmová vlákna (stejné odvození jako NotifCenter), ne přes ořezaný digest.
- **⚪ LOW** `Velin.tsx:191` — Sdílené úkoly (R2) v přehledu ukazují jen prvního přiřazeného [invariant]
  - Scénář: V řádcích Po termínu (ř. 191 `asgByTask.get(tk.id)?.[0]`) a ve feedu (ř. 235) se vybírá vždy jen první assignee z pole. U úkolů v režimu shared_any/shared_all (R2) s více přiřazenými se ve Velíně zobrazí iniciály/jméno jen jednoho (dle nedeterministického pořadí řádků assignments), takže vedení vidí odpovědnost přiřazenou nesprávné osobě. Ostatní přiřazení nejsou nijak naznačeni.
  - Oprava: U sdílených úkolů zobrazit více avatarů nebo označení „tým/N lidí“, případně řadit assignees deterministicky, aby výběr prvního nebyl náhodný.
- **⚪ LOW** `Velin.tsx:274` — Datum a hranice „po termínu“ zamrznou o půlnoci u dlouho otevřené karty [state]
  - Scénář: Vedoucí nechá Velín otevřený přes půlnoc a přes noc se nezmění žádná data (allTasks/assignments… drží stejnou referenci), takže se `view` memo ani `todayLabel` nepřepočítají. Hlavička pak ráno ukazuje včerejší datum a `isOver` používá zamrzlé `tdy` (včerejšek): úkol, jehož termín byl na den, který o půlnoci skončil, se NEoznačí jako „po termínu" (falešně negativní), dokud jakákoli interakce nebo změna dat nevyvolá re-render s aktuálním dnem. Návrh opravy (midnight timer / přepočet na focus/visibilitychange) je namístě.
  - Oprava: Odvozovat dnešní datum z tikajícího zdroje (interval/„midnight timer“) nebo znovupočítat při návratu focusu na okno.

## apps/web/src/lib/tasks.ts  (5)

- **🟡 MED** `tasks.ts:135` — shared_all větev předchází opakování/postup → recurring shared_all úkol se nedokončí ani neposune, chain se neposune [correctness]
  - Scénář: Create a task with a recurrence rule AND assignment_mode='shared_all' (reachable via QuickAdd with a recurrence token + ≥2 assignees, or via TaskDetailPanel). When participants check it off, toggleTask hits the shared_all branch (tasks.ts:135) before the recurrence branch (tasks.ts:216); once all are done (line 189) it sets tasks.completed_at and returns at line 209, so the recurrence row never advances to the next occurrence (R4 broken) — the task is permanently completed instead. Fix: for recurring tasks, perform the R4 row-advance inside the shared_all completion path (or explicitly disallow recurrence+shared_all). The separately-alleged 'chain not advanced on recurrence' (line 261) is NOT a bug: recurrence leaves completed_at NULL, and the chain derives its state from completed_at, so keeping the step open until the recurrence ends is correct.
  - Oprava: Vyjasnit prioritu: buď u recurring úkolů řešit posun řady i uvnitř shared_all/chain cesty, nebo recurring+shared_all explicitně zakázat. Na ř. 261 zvážit zavolání advanceChainForTask i při posunu řady, pokud je úkol krokem postupu.
- **🟡 MED** `tasks.ts:147` — Dokončení neúčastníkem a následné od-škrtnutí smaže reálná dokončení kolegů (shared_all) [data-loss]
  - Scénář: Předpoklad: shared_all úkol s účastníky A, B; A se sám odškrtne přes `mine` větev (ř. 183, assignments[A].completed_at=ts_A), B ne → tasks.completed_at zůstává null (odvozené). Manažer-neúčastník přepne úkol na Hotovo (přes detail cizího úkolu ř. 440 nebo hromadné Hotovo v BulkBar ř. 286): větev ř. 147 zachová ts_A a nastaví tasks.completed_at=ts. Když ho pak přepne zpět, task.completed_at je již ts → nowDone=false → nextAsg (ř. 152-155) nastaví VŠEM účastem null, čímž tiše smaže genuinní completed_at účastníka A. ⌘Z to obnoví (un-toggle undo drží prevAsg vč. ts_A), bez undo je to tichá ztráta per-osoba stavu.
  - Oprava: Při un-toggle neúčastníkem obnovit původní per-osoba stavy z historie, ne paušálně null; nebo neúčastníkovi nedovolit hromadné pře-/od-škrtnutí, které přepisuje cizí dokončení bez potvrzení.
- **⚪ LOW** `tasks.ts:183` — shared_all s jediným přiřazeným (=aktér) propadne do obecné větve mimo odvozený model (R2) [invariant]
  - Scénář: Create a shared_all task with two assignees (me + colleague), then in the detail panel remove the colleague via the ✕ (toggleAssign runs DELETE without changing assignment_mode), leaving a single assignment that is me. Toggle the main checkbox: because asg.length === 1 && mine, execution skips both shared_all branches (needs !mine or asg.length >= 2) and falls through to the general branch (line 213), which sets tasks.completed_at directly but leaves my assignments.completed_at = null. The detail panel then shows the task as done while the mode hint reads 0/1 and the person's BrassCheck is unchecked. Adding a second participant afterward makes the derivation diverge (task done, new participant not). Fix as proposed: unify the participant branch to asg.length >= 1 && mine so the sole assignee's completed_at is updated and task.completed_at is set derivably.
  - Oprava: Sjednotit podmínku na `asg.length >= 1 && mine`, aby i jediný účastník aktualizoval svou assignments.completed_at a task.completed_at nechal odvozeně.
- **⚪ LOW** `tasks.ts:328` — deadlineLabel má natvrdo české „do " (chybí i18n) [i18n]
  - Scénář: V EN lokalizaci se u deadline vlaječky na kartě úkolu (TaskItem/Board) zobrazí „do Fri 27. 6." místo anglického ekvivalentu (např. „by Fri Jun 27") — prefix „do" je natvrdo česky (tasks.ts:328) a formát data zůstává v pořadí „d. m.". Funkce deadlineLabel navíc nepřijímá t() param. Doporučení: napojit na existující klíč quickadd.deadlinePill (EN „by {{date}}", CS „do {{date}}") nebo zavést nový deadline.byPrefix a datum lokalizovat přes Intl, konzistentně s dueLabel.
  - Oprava: Prefix přetáhnout do i18n klíče (např. deadline.byPrefix) a formát data lokalizovat přes Intl.
- **⚪ LOW** `tasks.ts:183` — shared_all s jediným účastníkem = já propadá obecnou větví, assignments.completed_at se neaktualizuje [invariant]
  - Scénář: Shared_all úkol vznikne se 2+ přiřazenými (jinak nelze režim přepnout — tlačítka na TaskDetailPanel.tsx:1187 vyžadují asg.length>=2). V detailu se přes ✕ (toggleAssign, ř. 452) odeberou ostatní až zbydu jen já → režim zůstane shared_all (nikde se nevrací na single). Odškrtnu úkol hlavním checkboxem v seznamu (toggleTask(task, myId)). Protože asg.length===1 && mine, obě speciální větve (147 !mine, 183 >=2) se přeskočí a kód propadne do obecné větve, která nastaví tasks.completed_at, ale MOU assignments.completed_at nechá null. Když se pak přidá druhý účastník, úkol se jeví jako hotový, ale žádná účast dokončená není → nekonzistentní odvozený R2 stav. Nízká závažnost: úzký okrajový případ, okamžitý viditelný efekt (úkol hotový) odpovídá záměru, drift se projeví až při pozdějším přidání účastníka.
  - Oprava: Ošetřit i případ asg.length===1 && mine v shared_all větvi (nastavit i assignments.completed_at), nebo podmínku změnit na `asg.length >= 1 && mine`.

## apps/web/src/components/QuickAdd.tsx  (4)

- **🟡 MED** `QuickAdd.tsx:224` — No in-flight guard on submit() — rapid double Enter creates duplicate tasks [state]
  - Scénář: Uživatel dvakrát rychle stiskne Enter (nebo Enter a hned klikne na „Přidat") v okně mezi zahájením prvního awaited INSERTu a re-renderem, který vyčistí input. Protože není žádný in-flight guard a `raw`/`parsed.name` se mezi oběma stisky nezmění, oba běhy submit() projdou guardem, vyrobí každý jiné taskId a vloží dva identické úkoly (u ≥2 přiřazených i duplicitní assignments). Duplikát se syncne kolegům. Dopad je viditelný, ale vratný smazáním, a vyžaduje dvě akce v úzkém časovém okně — proto medium, ne high.
  - Oprava: Add a `submitting` ref/state set true at the top of submit() and bail if already true (reset in a finally); also disable the Add button while submitting.
- **🟡 MED** `QuickAdd.tsx:283` — Typed @person that resolves to nobody shows an assignee pill but is silently dropped on submit [correctness]
  - Scénář: Type "opravit @xyz" (or any @token whose letters don't match any member's name-substring or initials-prefix). Because no member matches, the autocomplete suggestion list is empty (sugRaw returns null), so the user cannot pick anyone; the parser still yields personQueries:["xyz"]. A "@xyz" assignment pill renders (QuickAdd.tsx:283-284), identical to a real assignee pill. On submit(), the people.find(...) at 151-157 returns nothing, so uniqueAssignees is empty and the task is inserted with zero assignments — the user believes someone was assigned, but nobody is, with no warning.
  - Oprava: Either resolve personQueries live and only show the pill when a real person matches, or surface an "unrecognized @xyz" warning before submit so the mismatch isn't silent.
- **🟡 MED** `QuickAdd.tsx:152` — @person is resolved by first substring match — ambiguous names assign the wrong colleague [correctness]
  - Scénář: Se dvěma členy „Jan Novák" a „Jana Dvořáková" uživatel napíše úkol tak, že @token není na konci vstupu (např. „@Jan zavolej klientovi") — tím se NEspustí našeptávač (ten reaguje jen na token na konci). Token zůstane jako neresolvovaný personQuery. Při odeslání (submit) se ř. 153-157 resolvuje přes people.find s podmínkou name.includes(ql), kde „jana dvořáková".includes("jan") je také true, takže odpovídají OBA lidé a find vrátí prvního v poli — úkol se tiše přiřadí libovolnému „Janovi" bez jakéhokoli signálu, kterého vybral. Totéž pro kolize iniciál (startsWith). Alternativně dosažitelné i s tokenem na konci přes Escape (setSugDismissed) + Enter.
  - Oprava: Require the user to pick from the autocomplete for assignment (don't auto-resolve free-text @queries), or when >1 candidate matches, refuse to auto-assign and prompt.
- **⚪ LOW** `QuickAdd.tsx:291` — Highlight overlay does not scroll-sync with the input — highlights drift on long input [a11y]
  - Scénář: User types a task name longer than the QuickAdd input's rendered width (e.g. "připravit prezentaci pro poradu ve středu ve 14:00 p1"). The <input> scrolls horizontally to keep the caret visible, but the aria-hidden overlay div (px-3 py-2, overflow-hidden, whitespace-pre) has no scroll/transform sync and remains at scrollLeft=0. The brass highlight boxes for recognized tokens (which typically appear at the end of the string) end up misaligned with, or clipped away from, the visible text — the highlighting looks broken. Purely cosmetic (overlay is aria-hidden); no functional or assistive-tech impact.
  - Oprava: Sync the overlay's transform/scrollLeft to the input's scrollLeft on scroll/input, or measure and translate the overlay, so the two stay aligned.

## apps/web/src/screens/Ukoly.tsx  (4)

- **🟡 MED** `Ukoly.tsx:130` — kbNav navigation nad CAP míří na nevykreslené řádky — Space/⌫ zasáhnou neviditelný úkol [keyboard]
  - Scénář: Pohled „Úkoly" (list mode) má >400 nedokončených úkolů → capped=true, DOM = prvních 400 (shownCapped/groupsCapped). Uživatel drží `j` (OS key-repeat), kbSel projede přes celý visualList až za index 400. Pro id>400 neexistuje vykreslený KbRow (žádný ring, kbNav nescrolluje). Uživatel stiskne Space → toggleTask na neviditelném úkolu (rows.find najde data), nebo Backspace → deleteTaskWithUndo(cur) smaže neviditelný úkol. Obě akce jsou vratné (toggle zpět / undo), ale provedou se naslepo na úkolu mimo obrazovku i mimo DOM.
  - Oprava: Předávej do useKbNav/setNavIds jen skutečně vykreslený seznam (shownCapped / groupsCapped.flatMap), nebo přidej scrollIntoView + auto-rozšíření capu při navigaci za hranici; případně zakázat mutace na kbSel mimo viewport.
- **🟡 MED** `Ukoly.tsx:203` — Nástěnka (Board) obchází CAP 400 — vykreslí neomezeně karet [perf]
  - Scénář: Uživatel s velkým workspace (tisíce otevřených top-level úkolů) bez aktivního filtru projektu (?projekt=) přepne pohled na Nástěnku. Ukoly.tsx předá Boardu neořezaný `shown` (CAP=400 se týká jen list/groups větve). Board rozdělí všechny úkoly do sloupců a vykreslí každou kartu bez virtualizace → desetitisíce DOM uzlů najednou, dlouhý jank/zamrznutí. Jde o perf regresi omezenou na okrajový velký-workspace stav, ne o chybu korektnosti.
  - Oprava: Aplikovat obdobný cap/virtualizaci i na Board (per sloupec), nebo předávat Boardu ořezaný seznam.
- **⚪ LOW** `Ukoly.tsx:93` — Počet v hlavičce sekce nesouhlasí s vypsanými řádky při zapnutém „Dokončené" [correctness]
  - Scénář: Otevři modul Úkoly bez filtru projektu (skupinový výpis). V projektu měj např. 2 aktivní top-level úkoly a 3 dokončené top-level úkoly. Zapni v toolbaru „Dokončené". Pod hlavičkou projektu se vypíše 5 řádků (aktivní + dokončené), ale číslo v hlavičce ukáže jen „2" (totals přeskakuje completed_at). Číslo je tedy menší než počet viditelných řádků. Nezávisle: i bez showDone `total` ignoruje aktivní filtry priorita/stav/osoba i fulltext hledání, takže po zafiltrování `list` číslo neodpovídá počtu vypsaných řádků. Oprava: počítat total ze stejně filtrovaného základu jako `shown`/`list` (respektovat showDone i toolbar filtry a searchQ), nebo zobrazit „X z Y".
  - Oprava: Buď počítat total ze stejně filtrovaného základu jako `list` (respektovat showDone a toolbar filtry), nebo zobrazit „X z Y", ať je vztah čísla k výpisu jednoznačný.
- **⚪ LOW** `Ukoly.tsx:136` — „Zobrazit vše" (showAllRows) přetrvá při přechodu mezi projekty a znovu-nasadí neomezený render [state]
  - Scénář: Uživatel je v projektu s >400 nedokončenými úkoly (shown.length > CAP=400) → klikne na patičku „zobrazit vše" (setShowAllRows(true)). Poté klikne v banneru na odkaz „Všechny úkoly" (search={{}}), který mění pouze projekt search param → Ukoly se neremountuje → showAllRows zůstává true → `capped` = false → CAP 400 se neuplatní a vykreslí se všechny řádky napříč všemi projekty (řádově tisíce DOM uzlů), přesně proti účelu capu. Jde o výkonovou degradaci v edge-case (nutná specifická navigační sekvence + velký dataset), nikoli funkční/datovou chybu — proto low.
  - Oprava: Resetovat showAllRows při změně projektId (useEffect na projektId nebo klíčovat komponentu podle projektId).

## apps/web/src/screens/Projekty.tsx  (4)

- **🟡 MED** `Projekty.tsx:446` — ProjectCard: úkol s termínem DNES se po půlnoci/odpoledne zobrazí jako „zbývá -1 dní" a červeně [correctness]
  - Scénář: Projekt s kind=goal nebo cycle a delivery_date=dnešek: od 12:00 do půlnoci lokálního času se na ProjectCard zobrazí „zbývá -1 dní" červeně (var(--w-overdue)), přestože termín je dnes. Příčina: dueDays = Math.round((lokálníPůlnoc − Date.now())/den) překlopí na −1, jakmile od půlnoci uplyne >12 h. Oprava: počítat rozdíl kalendářních dnů (obě strany na lokální půlnoc, Math.ceil nebo porovnání dnů), aby termín==dnes dal 0.
  - Oprava: Počítat rozdíl mezi kalendářními dny (obě strany na lokální půlnoc a použít Math.ceil / porovnání ISO řetězců), ne round nad milisekundovým rozdílem; termín == dnes ať je 0 dní.
- **🟡 MED** `Projekty.tsx:477` — ProjectCard: natvrdo česká věta „do X. Y. · zbývá N dní" bez i18n, bez plurálu, se zápornou hodnotou [i18n]
  - Scénář: Řetězec `do {getDate()}. {getMonth()+1}. · zbývá {dueDays} dní` je napsán natvrdo (mimo mail modul, kde je čeština záměr). V EN locale zůstane česky. „dní" je použito pro všechny počty (má být 1 den / 2 dny / 5 dní), a při termínu po termínu se ukáže nesmyslné „zbývá -5 dní" místo např. „5 dní po termínu".
  - Oprava: Přesunout do i18n klíčů s plurálem (count) a datum formátovat přes Intl; pro záporné dueDays zobrazit samostatný text „po termínu".
- **⚪ LOW** `Projekty.tsx:546` — projects.members nemá české plurálové tvary → „1 členů" [i18n]
  - Scénář: t("projects.members", { count: members }) se renderuje jen jako „{{count}} členů" — v cs.json existuje pouze klíč projects.members bez tvarů _one/_few/_many. Projekt s 1 členem ukáže „1 členů", se 3 členy „3 členů" (má být „1 člen", „3 členové").
  - Oprava: Doplnit projects.members_one/_few/_many (a EN _one/_other) v obou locales.
- **⚪ LOW** `Projekty.tsx:74` — Počty na kartě projektu zahrnují podúkoly i netriážované položky schránky [correctness]
  - Scénář: Karta projektu (Projekty.tsx:74-85, 97-131) i detail (ProjectDetailPanel.tsx:61-64) agregují všechny řádky `tasks` daného project_id. Podúkoly dědí project_id rodiče (TaskDetailPanel.tsx:472, AddTaskModal.tsx:751), takže se do „otevřených · N celkem" započítají, ačkoli seznam úkolů projektu (Ukoly.tsx:70,94) top-level filtruje podúkoly pryč. Nejsilnější případ: karta inbox projektu započítá netriážované inbox úkoly bez termínu, přestože inbox.ts:32-36 dokumentuje, že do počtů nepatří — přímý rozpor s invariantem. Jde o kosmetické nafouknutí čísel v UI, žádná ztráta/poškození dat.
  - Oprava: Rozhodnout se pro konzistentní sémantiku: pokud mají počty odpovídat seznamu, přidat do agregace `parent_id IS NULL` a vyloučit netriážované inbox úkoly.

## apps/web/src/lib/keyboard.tsx  (4)

- **🟡 MED** `keyboard.tsx:94` — Globální zkratky (q, /, ?, g+písmeno, ⌘Z) nejsou gatované otevřenými vrstvami — jen typing/onMail [keyboard]
  - Scénář: Otevři app-level NotifCenter (klik na zvonek v hlavičce) na jakékoli ne-mailové obrazovce. Focus zůstane na tlačítku zvonku (ne input), panel nenese data-esc-layer ani focus-trap → globální keyboard handler není gatovaný. (a) Stisk `q` otevře AddTaskModal NAD panelem (stacking dvou vrstev). (b) `g`+`m`/`u` odnaviguje celou aplikaci pryč, i když je vrstva „otevřená". (c) ⌘Z spustí globální undo() poslední operace úkolu, zcela nesouvisející s obsahem panelu — matoucí, byť vratné přes redo. Stejný leak platí i nad TaskDetailPanel, jakmile focus není v inputu (klik na tlačítko v detailu). Oprava: po typing/onMail guardu přidat `if (openId || document.querySelector('[data-esc-layer]')) return;` (mimo větve Escape a ⌘K), stejně jako kbNav/BulkBar/TaskDetailPanel. POZNÁMKA k závažnosti: dopady jsou z velké části vratné (modal nad panelem, navigace, redo-vatelné undo). Tvrzení o „zahození neuloženého AddTask draftu" je slabší — AddTaskProvider drží modal na kořeni nad routerem, takže g-navigace ho neodmountuje, a detail úkolu je hlasitý autosave (žádný draft se neztrácí). Bez potvrzené trvalé ztráty dat → medium, ne high.
  - Oprava: V handleru keyboard.tsx po typing/onMail guardu přidat `if (openId || document.querySelector('[data-esc-layer]')) return;` (kromě větví Escape a ⌘K, které mají fungovat i nad vrstvami), stejně jako to dělá kbNav/BulkBar/TaskDetailPanel.
- **🟡 MED** `keyboard.tsx:67` — onMail early-return vypíná Escape → Cheatsheet otevřený a poté navigovaný na /mail nelze zavřít klávesou Esc [overlay-esc]
  - Scénář: Na /dnes stiskni `?` → otevře se Cheatsheet (keyboard.tsx:120). Bez zavření stiskni `g` pak `m` → globální handler (cheatOpen nic neblokuje, typing=false) nav.iguje na /mail, cheatOpen zůstává true a Cheatsheet je stále vykreslen (KeyboardProvider je nad Outlet). Na /mail: keyboard.tsx:67 `if (onMailRef.current) return;` skončí před Escape blokem; MailScreen.tsx:136 ustoupí kvůli data-esc-layer na Cheatsheetu; Cheatsheet nemá vlastní Esc handler → Escape nezavře tahák. Jediné zavření je klik na scrim/křížek. Rozpor navíc s tím, že tahák sám zobrazuje „Zavřít: Esc".
  - Oprava: Buď vyřídit Escape (a zavření cheat/palette) PŘED onMail guardem, nebo dát Cheatsheetu vlastní keydown Esc handler (jako mail/CheatSheet.tsx). Ideálně obojí.
- **⚪ LOW** `keyboard.tsx:75` — ⌘K otevře command paletu i nad již otevřeným Cheatsheetem/modalem (vrstvy se nevylučují) [state]
  - Scénář: ⌘K je zpracováno hned, bez ohledu na cheatOpen nebo jiné vrstvy. Stiskni `?` (Cheatsheet, z-index 71) a pak ⌘K → paleta (z-index 73) se otevře NAD tahákem; oba jsou zároveň mountované. Následný Esc: globální handler zavře nejdřív cheat (ř. 70) a paletin vlastní onKeyDown zavře paletu, takže jediný Esc zavře obě vrstvy najednou, což je matoucí.
  - Oprava: Před otevřením palety zavřít ostatní vrstvy (setCheatOpen(false)), případně ⌘K ignorovat, když je otevřený jiný [data-esc-layer].
- **⚪ LOW** `keyboard.tsx:97` — Po stisku `g` je následující klávesa vždy spolknuta, i když netvoří g-zkratku [keyboard]
  - Scénář: Po `g` se gPending nastaví na true; jakákoli další klávesa v okně 1200 ms projde větví ř. 97-110, resetuje gPending a při neznámém cíli jen `return` (ř. 109). Když uživatel omylem klepne `g` a pak chce `q` (nový úkol), `q` se ztratí (a musí ho stisknout znovu). Podobně `g` pak `?` nespustí tahák.
  - Oprava: Při neznámém cíli po `g` nespotřebovat klávesu — nechat ji propadnout do standardního zpracování (nebo alespoň znovu vyhodnotit jako běžnou zkratku).

## apps/web/src/screens/Prehled.tsx  (3)

- **🟡 MED** `Prehled.tsx:495` — Grid layout přetéká na úzkých telefonech (~360px) [mobile]
  - Scénář: Kontejner má `padding:'18px 22px'` (44px horizontálně) a grid `gridTemplateColumns: repeat(auto-fit, minmax(330px,1fr))`. Na Androidu 360px je dostupná šířka jen 316px < 330px minima → auto-fit vytvoří jeden sloupec o 330px, který přeteče a způsobí horizontální scroll celé stránky. Výchozí layout může být 'grid' z localStorage, takže feed fallback nepomůže.
  - Oprava: Snížit min track (např. minmax(min(100%,330px),1fr)) nebo přepnout na jednosloupcový/feed layout pod breakpointem.
- **⚪ LOW** `Prehled.tsx:230` — Feed „Dění týmu" porovnává UTC datum completed_at s lokálním dneškem [correctness]
  - Scénář: Jde o čistě zobrazovací overview widget „Dění týmu" (max 3 položky), ne o ukládání dat ani stavovou logiku úkolu. V kladném UTC offsetu úkol dokončený v úzkém okně po půlnoci lokálně (cca půlnoc až offset-hodina) buď z dnešního feedu vypadne, nebo se v něm objeví úkol z pozdního večera pomýleně jako 'dnešní'. Dopad je kosmetický off-by-a-day v přehledu, bez poškození dat — proto navrhuji snížit závažnost z medium na low. Oprava dle návrhu (formátovat completed_at do lokálního data, např. Intl 'en-CA' YYYY-MM-DD, místo slice nad UTC ISO) je správná.
  - Oprava: Formátovat completed_at do lokálního data před porovnáním (např. `new Date(tk.completed_at).toLocaleDateString('sv')` nebo Intl s 'en-CA' pro YYYY-MM-DD), místo slice(0,10) nad UTC ISO.
- **⚪ LOW** `Prehled.tsx:329` — Tlačítko „Přeplánovat zpožděné" umožňuje dvojí spuštění (2× undo, 2× toast) [state]
  - Scénář: Uživatel dvakrát rychle klikne na „Přeplánovat zpožděné" před re-renderem. Oba kliky zachytí stejné view.ovd (žádný guard/disabled, closure se mezi kliky nemění). Proběhnou dva zápisy (idempotentní — oba nastaví due_date na dnešek, oba prev = původní hodnota), dva pushUndo záznamy a dva toasty. Data jsou správná, ale zobrazí se dva toasty a Zpět (⌘Z) je nutné zmáčknout dvakrát; druhé undo je no-op restore na stejnou hodnotu + vlastní toast.
  - Oprava: Přidat běhový guard (např. useRef/isRunning) nebo tlačítko po kliknutí deaktivovat do dokončení.

## apps/web/src/components/ProjectDetailPanel.tsx  (3)

- **🟡 MED** `ProjectDetailPanel.tsx:259` — Archivace projektu ho učiní trvale nedostupným (žádný archivní pohled) [data-loss]
  - Scénář: V detailu projektu uživatel v sekci STAV zvolí „archive" (ProjectDetailPanel.tsx:259-266 → status='archive', archived_at=nyní). Po zavření panelu projekt zmizí z gridu Projekty i z Hledání (obojí přes useProjects() s WHERE status != 'archive'), a banner „Upravit projekt" v Úkoly se nevykreslí, protože projMap z useProjects() archivovaný projekt neobsahuje (activeProject=undefined). Protože openId detailu se nastavuje jen přes open() z těchto vstupů a neexistuje otevření z URL, k archivovanému projektu se přes UI už nelze dostat a odarchivovat ho — pouze zásahem do DB/syncu. Nejde o zničení dat (řádek projektu, sekce, statusy, členové i úkoly zůstávají; úkoly se dál zobrazují v globálních Úkolech pod skupinou „—"), ale o jednosměrnou past. Oprava: přidat do Projekty pohled/přepínač archivovaných s cestou k odarchivaci (analogicky Seznamy.tsx:382-411), případně nechat archivovaný projekt dohledatelný v Hledat.
  - Oprava: Přidat do Projekty přepínač zobrazení archivovaných (nebo alespoň nechat archivovaný projekt dohledatelný v Hledat) a cestu k odarchivaci; případně samostatný dotaz pro archiv.
- **🟡 MED** `ProjectDetailPanel.tsx:51` — Esc v ProjectDetailPanelu zavře i modal nad ním (dvojité zavření) [overlay-esc]
  - Scénář: Panel detailu projektu (z-40) má bezpodmínečný Esc handler na window bez kontroly `[data-esc-layer]`/`openId`. Na /projekty otevřu panel, zmáčknu `q` → AddTaskModal (z-75, data-esc-layer) nad ním. Jedno Esc: AddTaskModal se zavře a ZÁROVEŇ se zavře i panel projektu. Totéž s ⌘K (CommandPalette) nebo `?` (Cheatsheet).
  - Oprava: Přidat `if (document.querySelector('[data-esc-layer]')) return;` na začátek Esc handleru.
- **⚪ LOW** `ProjectDetailPanel.tsx:182` — Vymazání názvu projektu se neuloží, ale input zůstane prázdný (tichá ztráta editace) [empty-state]
  - Scénář: onBlur ukládá jen když `name.trim() && name !== project.name` (ř. 182-186). Když uživatel smaže název na prázdno (nebo jen mezery) a klikne jinam, patch se neprovede, ale lokální stav `name` zůstane prázdný — input ukazuje prázdno, zatímco DB drží starý název, dokud se `project` nezmění. Uživatel si myslí, že smazal název; žádná zpětná vazba, že prázdný název je zakázán.
  - Oprava: Při neplatném (prázdném) názvu na onBlur vrátit input zpět na project.name a/nebo ukázat validační hlášku.

## apps/web/src/components/BulkBar.tsx  (3)

- **🟡 MED** `BulkBar.tsx:286` — Hromadné „Hotovo" vytvoří N undo záznamů místo jednoho — ⌘Z vrátí jen poslední [undo]
  - Scénář: bulkDone volá `for (const tk of open) await toggleTask(tk, uid)` — každý toggleTask si pushne VLASTNÍ undo entry. Označím-li hromadně 10 úkolů jako hotové, vznikne 10 undo záznamů. Jedno ⌘Z vrátí jen poslední úkol; uživatel musí zmáčknout ⌘Z 10×. bulkDelete/bulkAssign/bulkTerm přitom dělají JEDEN undo záznam na celou dávku → nekonzistentní a matoucí (uživatel čeká, že ⌘Z vrátí celou akci).
  - Oprava: Zabalit dávku do jednoho undo záznamu (snapshot completed_at/status_id všech + apply/revert v jedné transakci), podobně jako applyColumn/bulkAssign.
- **🟡 MED** `BulkBar.tsx:281` — Hromadné akce nemají re-entrancy guard → dvojklik posune opakovanou řadu dvakrát [state]
  - Scénář: Bulk actions have no busy/re-entrancy guard and buttons stay enabled until each handler's trailing clear() unmounts the bar. Rapidly invoking a bulk action twice (double-click, or a second deliberate click while a multi-task batch is still processing) launches two concurrent handlers. In the common case both read the same original snapshot and write idempotently, yielding one advance but DUPLICATE undo entries and duplicate toasts. In a read-after-write race — most reachable with many tasks selected (the sequential for-await toggleTask loop keeps the bar mounted) or a delayed second click — the second loadSelected() reads a recurring task already advanced to its next occurrence and advances it again, silently skipping an occurrence (R4 violation). Fix: add a busy ref, early-return while an action is in flight, and disable/clear on start.
  - Oprava: Přidat `busy` ref/state a na začátku každé bulk akce early-return, dokud předchozí neběží; tlačítka během běhu disablovat.
- **🟡 MED** `BulkBar.tsx:153` — Single Esc both closes a Peek/Notif overlay AND clears the bulk selection [overlay-esc]
  - Scénář: On /ukoly (or any non-mail screen), select several task rows so BulkBar appears. Click the header bell to open NotifCenter (or open a PeekPanel from Přehled/Velín with a selection still active). Press Esc once: the overlay closes AND BulkBar.clear() discards the entire multi-select in the same keystroke, instead of a single-step cascade that closes the overlay first and preserves the selection. Root cause: components/NotifCenter.tsx and components/PeekPanel.tsx close on Esc without emitting the app-wide `data-esc-layer` marker (and without setting openId), so BulkBar's guard (BulkBar.tsx:153) does not detect that an overlay is open and its window-level Esc handler runs on the same event.
  - Oprava: Fixing the two overlays to emit data-esc-layer resolves this too, since BulkBar already respects that marker; alternatively make the Esc cascade strictly single-step.

## apps/web/src/mail/MailScreen.tsx  (3)

- **🟡 MED** `MailScreen.tsx:129` — ⌘K pro zavření hledání obchází close() — zůstane starý dotaz a zaseknutý chip „ve vlákně" [state]
  - Scénář: Otevři hledání ⌘K, napiš dotaz (např. „faktura"), zúž hledání na jedno vlákno přes ⌘F nebo tlačítko „Hledat ve vlákně" (nastaví m.soTh). Zavři hledání dalším ⌘K (ne Esc/klik mimo). SearchOverlay zůstává mountnutý (renderuje null), takže lokální q i m.soTh přežijí, protože ⌘K handler (ř. 129–133) volá jen setSearchOn(v=>!v) bez close(). Další ⌘K ukáže starý dotaz a stále aktivní chip „ve vlákně: …" a filtr (ř. 84) omezí výsledky jen na staré vlákno — uživatel netuší, že nehledá v celé poště. Esc a klik mimo tuto vadu nemají, protože resetují q + m.setSoTh(null).
  - Oprava: Sjednotit zavírání přes jedinou cestu: ⌘K při otevřeném overlayi volat close()/onClose se stejným resetem, nebo reset q+soTh navázat na `open===false` efektem v SearchOverlay.
- **🟡 MED** `MailScreen.tsx:158` — Klávesy j/k procházení seznamu rovnou otevírají vlákno a označují ho přečteným [keyboard]
  - Scénář: j/k (ř. 158–168) volají `mail.openThread(n)`, které kromě setSel dělá `setMstep("thread")` a `setOv(id,{read:true})` (state.tsx ř. 479–488). Na rozdíl od zvyklostí (j/k = jen posun kurzoru, o/Enter = otevřít) tady každé projetí seznamu klávesnicí otevře vlákno a nevratně ho označí jako přečtené; na mobilu navíc skočí do thread view. Nelze klávesnicí procházet, aniž bys maily „přečetl".
  - Oprava: Zavést samostatný kurzor pro j/k (jen setSel/scroll) a otevírat + značit přečteno až na o/Enter.
- **⚪ LOW** `MailScreen.tsx:129` — ⌘K otevře hledání i přes rozepsanou Novou zprávu → Esc pak zavře obě vrstvy [keyboard]
  - Scénář: Otevřu Novou zprávu (z-62, data-esc-layer), píšu do textarea. Zmáčknu ⌘K → větev na ř. 129 proběhne před typing/esc-layer guardem a přepne searchOn → SearchOverlay (z-74) se otevře nad compose oknem. Nyní jsou dva mail overlaye naráz. Jedno Esc → oba document Escape listenery (SearchOverlay ř. 44-48 a NewMessage ř. 89-92) zavolají onClose → zavřou se obě vrstvy současně. Bez ztráty dat: NewMessage si drží React state (po open=false vrací null, ale zůstává mountnutý) a navíc persistuje koncept do localStorage (D10, ř. 108-120), takže se rozepsaný text po znovuotevření vrátí. Návrh opravy (guard ⌘K přes if(document.querySelector('[data-esc-layer]')) return;) je vhodný — buď neotvírat search nad jiným esc-layerem, nebo dát Esc handlerům respekt k nejvyšší vrstvě.
  - Oprava: Před přepnutím search zkontrolovat `if (document.querySelector('[data-esc-layer]')) return;` (nebo alespoň neotvírat search, když už je otevřený jiný esc-layer).

## apps/web/src/mail/MailList.tsx  (3)

- **🟡 MED** `MailList.tsx:1229` — Ovládací prvky hlavičky seznamu jsou neklikatelné z klávesnice (span+onClick bez role/tabindex) [a11y]
  - Scénář: Uživatel na klávesnici (Tab) ani čtečka obrazovky se nedostane na hlavičkové ovládací prvky poštovního seznamu, protože jsou to `<span onClick>` bez role/tabIndex/onKeyDown (MailList.tsx:1209,1229,1273,1300,1350). Zvonek/Oznámení (1229), Ask Watson (1273) a Filtry/Zobrazení (1300) nemají žádnou náhradní klávesovou zkratku, takže jsou klávesnicí zcela nedosažitelné a čtečka je nehlásí jako tlačítka. Hledat (1209) a Napsat (1350) mají sice globální zkratky (⌘K//, resp. c v MailScreen.tsx:129,199), ale samotné prvky zůstávají nefokusovatelné a čtečkou neohlášené a zkratky nejsou pro AT/klávesnici objevitelné. Stejný vzor (span+onClick přes data-rowbtn/data-primary) je systémový napříč mail modulem.
  - Oprava: Použít <button> nebo doplnit role="button" + tabIndex={0} + onKeyDown (Enter/Space) na interaktivní spany hlavičky a lišty výběru.
- **⚪ LOW** `MailList.tsx:1674` — Hromadné „→ úkoly" spamuje toasty u vláken, která už úkol mají, a u 1 výběru nedá žádnou zpětnou vazbu [state]
  - Scénář: Bulk → úkoly (ř. 1674–1686) volá `m.quickTask(id)` pro každý vybraný. quickTask (state.tsx ř. 858–861) u vlákna s existující vazbou zobrazí toast „Vlákno už úkol má" — při výběru 5 už propojených vláken vyskočí 5 toastů. Zároveň souhrnný toast se ukáže jen `if (ids.length>1)`, takže u přesně jednoho vybraného vlákna proběhne akce bez jakékoli hlášky.
  - Oprava: V bulk cestě potlačit per-vlákno toasty quickTasku a vypsat jeden souhrn („X úkolů založeno, Y přeskočeno"), včetně případu jednoho vybraného.
- **⚪ LOW** `MailList.tsx:1012` — Menu Filtry/Zobrazení zůstává otevřené při otevření zvonku, Ask Watson, hledání či Napsat [overlay-esc]
  - Scénář: Otevři popover Filtry/Zobrazení (tlačítko ř. 1300 → setVmenu true), pak klikni na zvonek (ř. 1229, setNotifOn true), Ask Watson (ř. 1275), hledání (ř. 1211, onSearch) nebo Napsat (ř. 1350, onCompose). Všechna tato tlačítka jsou uvnitř vmenuRef, takže outside-click handler (ř. 1015–1016) menu nezavře a žádný z handlerů nevolá setVmenu(false). vmenu tedy zůstane true; během otevřeného overlaye (NotifCenter = fixed portál zIndex 64/65) je popover filtru překrytý, ale po zavření overlaye je menu Filtry nečekaně stále otevřené (stale open state).
  - Oprava: Zavírat vmenu při každé jiné akci hlavičky (setVmenu(false) v onCompose/onSearch/bell/ask) nebo zúžit vmenuRef jen na tlačítko + popover.

## apps/web/src/screens/Nadchazejici.tsx  (2)

- **🟡 MED** `Nadchazejici.tsx:214` — Kalendář dostává úkoly předfiltrované na >= dnes → minulost i zpožděné jsou v kalendáři neviditelné a plánovací panel „Zpožděné" je mrtvý [correctness]
  - Scénář: Na obrazovce Nadcházející se `tasks` ořízne na `>= dnes` (Nadchazejici.tsx:119) a předá do Calendaru (ř. 214). Uživatel v kalendáři odroluje šipkou/← na minulý týden nebo měsíc a nevidí žádné úkoly, které tam reálně mají termín (byly odfiltrovány; calTasks je jen `[...tasks]`, Calendar.tsx:360). Zároveň když v ozubeném menu zapne plánovací panel (defaultně vypnutý, PLANNING_LS), sekce „Zpožděné" a tlačítko „Přeplánovat" se nikdy nevykreslí, protože `overdue = tasks.filter(tIso < todayIso)` je vždy prázdné (Calendar.tsx:2340-2342). Dopad je omezen na tuto obrazovku: kalendář není výchozí pohled, panel je defaultně vypnutý, a overdue plánování funguje korektně na obrazovkách Úkoly a Oblíbené, kde `Calendar` dostává neořezaný seznam. Oprava: Calendaru na Nadcházející předávat neořezaný seznam (jen wsFilter/showDone), ořez na budoucnost nechat pouze pro bucketový seznam.
  - Oprava: Kalendáři předávat neořezaný seznam (allTasks bez `>= tdy`, pouze s wsFilter), případně jen s filtrem na completed dle showDone. Ořez na budoucnost nechat pouze pro bucketový seznam, ne pro kalendář ani PlanningPanel.
- **⚪ LOW** `Nadchazejici.tsx:210` — Kalendářový pohled se renderuje bez TasksToolbar, ale dostává úkoly filtrované toolbarem a hledáním → hotové úkoly v kalendáři vůbec nejsou a zbylý hledaný dotaz kalendář tiše vyprázdní [state]
  - Scénář: Kalendář (Nadchazejici.tsx:210-217) nemá TasksToolbar ani jiné ovládání viditelnosti hotových, takže při čerstvém vstupu do kalendáře je tb.showDone=false a hotové neopakované úkoly se nezobrazí. Odškrtnu-li reálný úkol přes CalCheck (→ completed_at), okamžitě zmizí a v kalendáři ho nejde znovu odškrtnout, protože chybí přepínač „Dokončené" (obnova jen přes globální ⌘Z nebo zapnutím showDone v Seznamu/Nástěnce a návratem). Done styling v kalendáři tak pro reálné neopakované úkoly zůstává prakticky nedostupný (živý je pro virtuální výskyty opakování). POZOR: původní „důsledek 2" (hledání tiše vyprázdní kalendář bez viditelného pole) neplatí — vyhledávací pole je globální v Headeru a je viditelné vždy, když je dotaz neprázdný.
  - Oprava: Pro kalendář předávat úkoly nefiltrované hledáním a nezávislé na skrytém showDone (nebo v kalendáři zobrazit vlastní ovládání viditelnosti hotových), aby šlo hotové úkoly vidět a odškrtnutí vrátit.

## apps/web/src/lib/undo.ts  (2)

- **🟡 MED** `undo.ts:119` — Smazání úkolu nesmaže/neobnoví task_user_colors → ztráta per-user barvy [data-loss]
  - Scénář: Uživatel nastaví per-user barvu úkolu (task_user_colors, R6 overlay) v detailu úkolu. Poté úkol smaže přes tahák „Smazat (s undo) ⌫" (deleteTaskWithUndo) nebo hromadně přes BulkBar (bulkDelete → deleteTasksWithUndo). Lokální DELETE se dotkne jen CHILD_TABLES + tasks, barvu nechá jako lokálního sirotka; undo snapshot barvu vůbec nezachytí. Server po nahrání delete op kaskádně (onDelete: cascade) smaže task_user_colors a smazání se přes bucket user_own stáhne dolů → lokální sirotek zmizí. ⌘Z (undo) reinsertuje tasks a CHILD_TABLES, ale task_user_colors nikdy → per-uživatelská barva je trvale ztracena a reinsertovaný úkol se nahraje bez barvy. Oprava: přidat task_user_colors do CHILD_TABLES (snapshot + DELETE + reinsert), aby delete i undo pokrývaly barvu stejně jako ostatní task-child data. Závažnost snížena na medium: ztracená hodnota je jediná barevná preference (snadno znovu nastavitelná UI nápověda), ne obsah úkolu/komentáře — byť tichá ztráta syncované per-user vrstvy je přesně to, co R6 hlídá.
  - Oprava: Přidat task_user_colors do CHILD_TABLES v undo.ts (snapshot + DELETE + reinsert), aby delete i undo pokrývaly barvy stejně jako bulkProject.
- **🟡 MED** `undo.ts:119` — deleteTasksWithUndo neošetřuje task_user_colors → osiřelé per-user barvy + nekonzistentní undo (R6) [data-loss]
  - Scénář: Task has a per-user color set (task_user_colors row synced locally + server). User deletes the task via detail panel, keyboard (Backspace), or bulk bar — all call deleteTasksWithUndo, which deletes tasks + CHILD_TABLES but leaves task_user_colors untouched locally. On upload the server DELETE FROM tasks cascades (ON DELETE cascade) and removes the server's task_user_colors row. User then presses Cmd+Z: undo reinserts tasks + CHILD_TABLES but NOT task_user_colors, so locally the color still appears (it was never deleted locally) and the task looks fully restored. However the server color row was cascade-deleted and undo never reinserts it; PowerSync sync-down then removes the unmutated local task_user_colors row to match the server. Net result: after delete+undo the task comes back but the user's per-user color is permanently lost, with a misleading window where it appeared preserved. (The pure delete-without-undo case only produces a transient local orphan that sync-down cleans up, and dropping the color there is arguably correct.) Fix: add task_user_colors to CHILD_TABLES so snapshot/DELETE/reinsert treat it like every other task child.
  - Oprava: Přidat 'task_user_colors' do CHILD_TABLES (snapshot + DELETE + reinsert), aby se mazání i undo chovaly stejně jako u ostatních dětí úkolu.

## apps/web/src/mail/FloatComposer.tsx  (2)

- **🟡 MED** `FloatComposer.tsx:218` — Odeslání z plovoucího composeru bezpodmínečně zavře okno a obejde kolizní i přílohovou pojistku [correctness]
  - Scénář: Uživatel otevře v plovoucím composeru rozepsaný koncept kolizního vlákna (t.coll — např. „faktura") přes chip, zatímco v hlavním pohledu je jiné vlákno. Klikne Odeslat: checkSend jen nastaví collArmed a return s toastem „…kliknutím znovu odešleš i tak", ale FloatComposer hned zavře okno (setFloat(null)). V hlavním pohledu je jiné vlákno, takže tlačítko „klikni znovu" pro toto vlákno nikde není; koncept sice přežije jako chip, ale každé znovuotevření a klik okno zase zavře (po 6 s se collArmed resetuje a znovu se zbrojí). Override kolizní pojistky je z floatu fakticky nefunkční a bez undo lišty může uživatel věřit, že mail odešel. (Pozn.: přílohová větev je proti tvrzení nálezu v pořádku — warn modal se korektně otevře v hlavním pohledu a odešle správné vlákno.)
  - Oprava: Zavřít float až podle výsledku checkSend (vrátit bool/ zjistit, zda se skutečně odeslalo), nebo nechat okno otevřené a zavírat ho z doSend přes efekt na undo/sent.
- **🟡 MED** `FloatComposer.tsx:188` — Esc v plovoucím composeru composer nezavře — místo toho zavře vlákno pod ním [overlay-esc]
  - Scénář: Uživatel čte vlákno (mstep="thread") a klikne na tlačítko „plovoucí okno" ve vlákně (MailThread.tsx:2806) → otevře se FloatComposer. Píše do jeho textarea a stiskne Esc v očekávání, že se plovoucí okno zavře/minimalizuje. Globální Escape handler v MailScreen běží i při fokusu v poli (Escape větev je před typing guardem), FloatComposer nemá data-esc-layer ani vlastní Esc, scr==="mail", výběr prázdný → spadne na `if (mail.mstep==="thread") mail.closeThread()`. Vlákno na pozadí se zavře (na mobilu viditelně přepne na seznam), plovoucí okno zůstane otevřené — opak očekávání. Oprava: přidat FloatComposeru vlastní Esc (minimalizace/zavření) a označit ho data-esc-layer.
  - Oprava: Přidat FloatComposeru vlastní Esc (minimalizace/zavření) a označit ho `data-esc-layer`, aby ho kaskáda Esc respektovala jako horní vrstvu.

## apps/web/src/mail/MailboxWizard.tsx  (2)

- **🟡 MED** `MailboxWizard.tsx:120` — Modaly bez focus-trapu a scroll-locku — Tab uteče za overlay, pozadí roluje [a11y]
  - Scénář: Klávesnicový uživatel otevře MailboxWizard. V krocích 1/3/4 jsou všechny ovládací prvky nefokusovatelné <span onClick>, takže uvnitř modalu není žádný tabovatelný prvek a první Tab přesune fokus rovnou na prvky stránky za scrimem (vizuálně zakryté) — fokus „zmizí". V kroku 2 (IMAP) jsou fokusovatelné <input>y, ale Tab za posledním z nich rovněž uteče do pozadí. Protože se nezamyká scroll <body>, kolečkem/šipkami nad scrimem roluje pozadí místo modalu. Stejný vzor (fixed overlay bez focus-trapu a scroll-locku) mají PersonCard a NotifCenter; jen AskWatson dává initial focus na input. Oprava: sdílený focus-trap (fokus dovnitř při otevření, cyklení Tab uvnitř, návrat na spouštěč po zavření) + overflow:hidden na <body> po dobu otevření libovolného data-esc-layer overlaye.
  - Oprava: Přidat sdílený focus-trap (fokus dovnitř při otevření, cyklení Tab uvnitř, návrat na spouštěč po zavření) a `overflow:hidden` na tělo po dobu otevření libovolného data-esc-layer overlaye.
- **⚪ LOW** `MailboxWizard.tsx:317` — Wizard krok 2 lze přeskočit bez ověření/testu — schránka se „připojí" bez autentizace [correctness]
  - Scénář: Otevři průvodce → krok 1 Gmail → Pokračovat → na kroku 2 NEklikni „Přihlásit" → Pokračovat → krok 3 → Připojit: zobrazí se „Schránka připojena", ačkoli demo-přihlášení neproběhlo. U IMAP totéž: „Pokračovat" se ukazuje i na kroku 2 (podmínka step<3) a nekontroluje `tested`, takže projde i s tested===null nebo "fail". Navíc po úspěšném testu 993 lze změnit port na 143 — onChange (ř. 237) neresetuje `tested`, zůstane stale "ok". Pozn.: jde o čistě demo bez mail backendu (viz komentáře ř. 196, 297), takže nedochází k reálnému obejití autentizace ani k úniku hesla/tokenu — dopad je pouze na věrnost průvodce/UX (uživatel může přeskočit centrální krok přihlášení). Oprava: na kroku 2 podmínit „Pokračovat" (OAuth auth===true, IMAP tested==="ok") a při editaci portu/serveru resetovat tested na null.
  - Oprava: Na kroku 2 podmínit „Pokračovat" stavem: pro OAuth `auth===true`, pro IMAP `tested==="ok"`; při editaci portu/serveru po úspěchu resetovat `tested` na null (jako to dělá onChange poskytovatele na kroku 1).

## apps/web/src/screens/Today.tsx  (1)

- **🟡 MED** `Today.tsx:229` — Bulk "Přeplánovat zpožděné" overwrites due_date directly — drops time-of-day and can move a recurring series without R4 prompt [invariant]
  - Scénář: rescheduleOverdue (Today.tsx:236-243) rewrites due_date for every g.overdue row with no recurrence guard, unlike the sibling paths BulkBar.tsx:199, TaskItem.tsx:75, TaskDetailPanel.tsx:932 which all skip/block recurring rows to preserve R4 (tento / tento a další / celá řada). A recurring task can reach g.overdue when its series has ended (recurrence until/count exhausted so expandOccurrences returns no next → Today.tsx:150-151 pushes the row with its original past due_date) or when its recurrence_rule fails to parse (parseRecurrenceRule→null at line 125 while the column is non-null → row falls to line 155). Normal active recurring tasks are instead projected onto today (142-149) and do NOT appear in overdue. When such a recurring row is present, the bulk reschedule silently rewrites its recurrence anchor date without the R4 prompt and without the recurringSkipped toast the other paths show. Mitigations: the change is undoable via pushUndo, and the separately-claimed time-of-day reset is intended behavior (rescheduleDate returns date-only across the whole app), so that part is not a defect.
  - Oprava: Preserve each task's original time-of-day when only the date changes, and route recurring rows (recurrence_rule not null) through the R4 this/series prompt instead of a blind due_date UPDATE.

## apps/web/src/lib/kbNav.tsx  (1)

- **🟡 MED** `kbNav.tsx:29` — Klávesové zkratky seznamu střílí i s otevřeným filtračním/řadicím popoverem [keyboard]
  - Scénář: Na obrazovce Úkoly/Dnes/Nadcházející uživatel klávesami j/k vybere řádek (kbSel se nastaví), pak otevře toolbar dropdown „Filtr" a klikne na pilulku priority/stavu — fokus je teď na <button> uvnitř popoveru, který NENÍ označen data-esc-layer a popover se po kliku nezavírá. Jakýkoli následný stisk klávesy probublá na window listener kbNav: Backspace smaže vybraný úkol (deleteTaskWithUndo), Space ho přepne (toggleTask), 1–4 změní prioritu — vše zatímco uživatel vizuálně interaguje s filtrem. Esc popover nezavře (nemá handler), jen vymaže kbSel. Závažnost medium místo high: destruktivní akce (smazání, priorita) mají undo a scénář vyžaduje předem nastavený kbSel přes klávesnici, takže nejde o bezpodmínečnou tichou ztrátu dat, ale o reálnou konzistentní chybu (všechny ostatní overlaye data-esc-layer mají). Oprava dle návrhu je správná: přidat na oba kontejnery (ř. 374 a 613) data-esc-layer + Escape handler, který zavře `open`.
  - Oprava: Přidat na oba dropdown kontejnery `data-esc-layer` (aby kbNav i ⌘K couvly) a Escape handler, který zavře `open`. Ideálně i focus management.

## apps/web/src/screens/Oblibene.tsx  (1)

- **🟡 MED** `Oblibene.tsx:43` — Oblíbené (P1 / mně) zobrazují i netriážované úkoly ze schránky [invariant]
  - Scénář: Uživatel přidá do osobní Schránky (projekt „Doručené"/„Inbox") úkol bez termínu s prioritou 1 (např. quick-add „něco p1") nebo úkol bez termínu ve Schránce přiřazený sobě. Otevře sidebar filtr Oblíbené → Priorita 1 (mode p1), resp. Přiřazeno mně (mode me). Netriážovaný úkol Schránky se v seznamu zobrazí a započítá do počítadla (ř. 79), přestože dle isInboxText/R8 do seznamů/počtů nepatří, dokud jej uživatel nenaplánuje nebo nepřesune. Vzniká nekonzistence oproti Dnes (Today.tsx ř. 110) a Úkolům (Ukoly.tsx ř. 56), které tyto položky filtrují. Oprava: importovat isInboxTask/inboxProjectIds a vyfiltrovat isInboxTask(tk, inboxProjectIds(projects)) ve `shown` před zobrazením.
  - Oprava: Aplikovat stejnou výjimku jako jinde: vyfiltrovat isInboxTask(tk, inboxProjectIds(projects)) před zobrazením v Oblíbené.

## apps/web/src/screens/Hledat.tsx  (1)

- **🟡 MED** `Hledat.tsx:95` — Hledání vrací i dokončené úkoly bez jakéhokoli odlišení [correctness]
  - Scénář: Uživatel dokončí úkol (completed_at je dle R9 nastaveno). Otevře obrazovku Hledat a zadá název úkolu. Dokončený úkol se objeví v sekci „Úkoly" vizuálně identicky s otevřenými (barevný puntík + název, žádný přeškrt ani šedé odlišení) a je klikatelný — otevře detail. Uživatel hledající aktivní úkol tak dostane matoucí směs hotových a nehotových položek. Sloupec completed_at je přitom už načten v SELECTu, ale ve filtru/renderu se vůbec nevyužije.
  - Oprava: Buď dokončené vyfiltrovat, nebo je vizuálně odlišit (přeškrt/šedě) a případně řadit až za otevřené; využít již načtené completed_at.

## apps/web/src/layout/AppLayout.tsx  (1)

- **🟡 MED** `AppLayout.tsx:83` — Mobilní main má paddingBottom:58 bez env(safe-area-inset-bottom), ale MobileTabBar výšku o safe-area zvětšuje → spodní obsah je oříznutý za lištou [mobile]
  - Scénář: Na iPhonu s home indikátorem (safe-area-inset-bottom ~34px, aktivní díky viewport-fit=cover v index.html): neprůhledná MobileTabBar zabírá ~89px od dna (obsah ~55px + safe-area ~34px), zatímco main rezervuje jen pevných 58px paddingBottom bez safe-area členu. Po odscrollování na dno zůstává spodních ~30px scrollovatelného obsahu (poslední úkol nebo tlačítko) schovaných za lištou a nelze na ně dosáhnout ani kliknout. Oprava: paddingBottom = calc(58px + env(safe-area-inset-bottom)), konzistentně s výpočtem sheetu „Více" na MobileTabBar.tsx:78.
  - Oprava: Nastavit paddingBottom na `calc(58px + env(safe-area-inset-bottom))` (nebo změřenou výšku lišty vč. safe-area), konzistentně s výpočtem v MobileTabBar sheetu (ř. 78).

## apps/web/src/mail/TaskModal.tsx  (1)

- **🟡 MED** `TaskModal.tsx:37` — isoPlusDays používá toISOString (UTC) — termín P1 „dnes“ přeteče na zítřek večer v Praze [i18n]
  - Scénář: Otevřu Email→úkol nad P1 (nebo P2) vláknem v NOČNÍCH hodinách po půlnoci v Praze — přibližně 00:00–02:00 letního času (CEST, UTC+2) nebo 00:00–01:00 zimního času (CET, UTC+1). V tomto okně je lokální datum už dnešní/nové, ale UTC je stále včerejší. isoPlusDays(0) = new Date().toISOString().slice(0,10) vrátí VČEREJŠÍ UTC datum → předvyplněný TERMÍN „z urgence · dnes“ pro P1 ukáže a při vytvoření uloží (dueISO, ř. 96) VČEREJŠEK, tedy rovnou úkol po termínu. isoPlusDays(1) pro P2 pak ukáže dnešek místo zítřka. (Pozn.: původně uvedený opačný scénář „večer → zítřek“ pro Prahu nenastává, protože Praha je před UTC.)
  - Oprava: Formátovat lokální datum: použít d.getFullYear()/getMonth()/getDate() s ručním zero-padem, ne toISOString().slice(0,10).

## apps/web/src/mail/mail.css  (1)

- **🟡 MED** `mail.css:206` — Full Screen (lcol) + zmenšení na mobil skryje oba panely bez možnosti návratu [mobile]
  - Scénář: Na desktopu otevři vlákno a zapni Full Screen čtení (lcol=true) tlačítkem na děliči; poté zmenši okno pod 880 px (responsivně). Pokud jsi ve stavu mstep="list" (buď jsi vlákno na desktopu zavřel před zmenšením, nebo po zmenšení klepneš na mobilní tlačítko zpět data-moonly v hlavičce vlákna, které volá closeThread→setMstep("list")), pak: listpane skryje ř. 206 (lcol=true, bez media guardu) a threadpane skryje ř. 198 (@media max-width:879px). Mailová plocha je prázdná. Obě ovládání pro reset lcol (toggle i rzReset) jsou uvnitř [data-tabup], které ř. 196 na mobilu skrývá; hamburger složek je uvnitř skrytého listpane. Uživatel se z prázdné plochy uvnitř mailu nedostane — jediný únik je opustit celý modul (unmount resetuje lcol na false). Oprava: omezit ř. 206 na @media (min-width:880px), nebo přidat resize/matchMedia listener, který při přechodu pod 880 px nastaví setLcol(false).
  - Oprava: Omezit ř. 206 na desktop (`@media (min-width:880px)`) nebo při přechodu do mobilního layoutu lcol resetovat na false.

## apps/web/src/mail/NewMessage.tsx  (5)

- **⚪ LOW** `NewMessage.tsx:172` — Klíč přílohy z indexu způsobí kolizi po přidání→odebrání→přidání a smaže obě [correctness]
  - Scénář: Přílohy jsou mock/placeholder (napevno `dokument_N.pdf · 118 kB`, žádný reálný file picker ani data). Reprodukce: přidej 2 přílohy → odeber první → přidej třetí; nová dostane opět `dokument_2.pdf · 118 kB`. Důsledek: duplicitní React key (console warning) a kliknutí na × u jedné přílohy smaže obě stejnojmenné. Protože jde o falešné placeholder přílohy v demo composeru (žádný reálný soubor se neztratí), reálný dopad je UI-only glitch → závažnost low, nikoli medium. Oprava dle návrhu (stabilní id přes crypto.randomUUID, klíčování/odebírání podle id) je správná.
  - Oprava: Ukládat přílohy jako objekty se stabilním id (crypto.randomUUID) a klíčovat/odebírat podle id, ne podle zobrazeného názvu.
- **⚪ LOW** `NewMessage.tsx:150` — Hlídání přílohy se spustí i na negaci („nepřikládám") [correctness]
  - Scénář: Do těla Nové zprávy se napíše negace typu „Nic nepřikládám, pošlu později" (nebo „nepřiložím soubor") a nepřipojí se žádná příloha. Po kliknutí na „Odeslat" (trySend, ř. 149) regex ATT_RE najde podřetězec „přikládám"/„přiložen" uvnitř negovaného tvaru „ne…", nastaví warnAtt=true a přeruší odeslání inline varováním „Zmiňuješ přílohu — ale žádná není připojená". Varování je ale obejitelné tlačítkem „Poslat i tak" (ř. 567), takže nejde o tvrdý blok, jen o rušivý falešný poplach. Oprava: přidat hranice slov / vyloučit prefix „ne" (např. /(?<!ne)přikládám/ apod.) nebo tvary negace ošetřit zvlášť.
  - Oprava: Zpřesnit regex (hranice slov, vyloučit negace ne-…) nebo warning brát jen jako nenápadný náznak, ne blokující modal.
- **⚪ LOW** `NewMessage.tsx:89` — Esc v Nové zprávě zavře popover šablon i celé okno naráz [overlay-esc]
  - Scénář: Otevřu Novou zprávu, rozepíšu text, kliknu 'Šablony' (setTplOpen(true)), zmáčknu Esc. Handler na ř. 89-92 bezpodmínečně provede setTplOpen(false) i onClose() → zavře se popover I celé compose okno naráz, místo aby první Esc zavřel jen popover (jak dělá klik mimo, ř. 99-106). Rozepsaný text se ale NEZTRATÍ — díky persistenci konceptu (localStorage 'watson-mail.newDraft', ř. 110-120) se při dalším otevření obnoví. Reálný dopad je tedy jen nečekané zavření okna, ne ztráta kontextu.
  - Oprava: Kaskádovat: `if (tplOpen) { setTplOpen(false); return; } onClose();`.
- **⚪ LOW** `NewMessage.tsx:88` — Esc zavře celé okno Nová zpráva i když je otevřený jen popover Šablony/Podpis [overlay-esc]
  - Scénář: V okně Nová zpráva otevřu popover „Šablony“ (nebo Podpis) a stisknu Esc, protože chci zavřít jen dropdown. Handler (ř. 88-92) ale bezpodmínečně udělá `setTplOpen(false); onClose();` — zavře i celé okno kompozice. Uživatel přijde o rozepsaný kontext na obrazovce (koncept se sice ukládá do LS, ale okno zmizí a musí se znovu otevírat). Standardní Esc-pořadí by mělo nejdřív zavřít jen nejvyšší popover.
  - Oprava: V Esc handleru nejdřív ošetřit `if (tplOpen) { setTplOpen(false); return; }` (a totéž pro popover podpisu) a teprve při zavřených popoverech volat `onClose()`.
- **⚪ LOW** `NewMessage.tsx:149` — Odeslání dovoleno i s prázdným příjemcem / předmětem / tělem [empty-state]
  - Scénář: V okně Nová zpráva nechám pole Komu, Předmět i tělo prázdná a kliknu Odeslat. `trySend` (ř. 149-155) kontroluje jen slíbenou přílohu; `doSend` (ř. 144) rovnou zobrazí „Odesláno z …“ a resetne formulář. Prázdný mail (bez příjemce) tak projde bez jakéhokoli varování — nekonzistentní s jinak pečlivými pojistkami (externí příjemce, příloha).
  - Oprava: V `trySend` přidat validaci `if (!to.trim()) { /* zvýraznit pole Komu / toast */ return; }`, případně i varování u prázdného předmětu.

## apps/web/src/components/CalendarWidget.tsx  (3)

- **⚪ LOW** `CalendarWidget.tsx:34` — Kalendářový widget ignoruje filtr firmy [correctness]
  - Scénář: Na Přehledu vyberu chip konkrétní firmy. Karty Dnes/Cíle/Pošta se zúží na danou firmu (fOk), ale kalendářový widget dál ukazuje tečky (červená=po termínu, mosazná=otevřené) pro dny s termíny ze VŠECH workspace včetně osobního inboxu, protože jeho dotaz nemá filtr workspace. Vidím tak „červený den" i tam, kde vybraná firma žádný úkol nemá; po kliknutí denní peek (DayPeek) navíc také nefiltruje podle firmy a zobrazí úkoly jiných firem. Návrh opravy (předat `firm` + mapu projekt→workspace a filtrovat řádky stejně jako fOk) platí; pro plnou konzistenci by se měl stejně filtrovat i DayPeek.
  - Oprava: Předat `firm` a mapu projekt→workspace do widgetu a filtrovat řádky podle workspace úkolu (stejně jako fOk v Prehled).
- **⚪ LOW** `CalendarWidget.tsx:121` — Navigační šipky kalendáře mají aria-label jen interpunkční glyf [a11y]
  - Scénář: Uživatel čtečky obrazovky projde měsíční navigaci kalendářového widgetu (CalendarWidget.tsx). U tlačítek předchozí/další měsíc (ř. 121, 130) čtečka místo účelu přečte název interpunkčního znaku („single left/right-pointing angle quotation mark"). U tlačítka skoku na dnešek (ř. 103–116) není aria-label žádný a čtečka přečte jen holé číslo dne s tečkou (např. „12.") bez vysvětlení funkce. Oprava: doplnit aria-label z existujících i18n klíčů (t('calendar.prev')/t('calendar.next')/t('calendar.today')).
  - Oprava: Nastavit srozumitelné aria-label (t('calendar.prevMonth')/nextMonth/today).
- **⚪ LOW** `CalendarWidget.tsx:34` — Calendar widget loads every task with a due date on every mount despite only showing one month [perf]
  - Scénář: Dotaz vybírá `due_date`/`completed_at` pro VŠECHNY úkoly s termínem (bez datového rozsahu), i když widget zobrazuje jeden měsíc; `byDay` pak drží agregát za každý termínový den napříč všemi měsíci — komentář „jen zobrazený měsíc" na ř. 37 je nepřesný. Na velkém workspace se do JS paměti materializuje celá množina termínovaných úkolů. Korekce oproti původnímu scénáři: nejde o fetch „na každém renderu" — `usePsQuery` je živá subskripce, běží při mountu a re-runuje jen při změně dat; a jde o lokální SQLite dotaz, ne server. Neohraničenost je částečně záměr (okamžitá navigace mezi měsíci bez re-query). Oprava: omezit na viditelný měsíc (`substr(due_date,1,7) = ym`) a re-query při změně měsíce, nebo agregovat server-side — s vědomím ztráty plynulosti navigace.
  - Oprava: Constrain the query to the visible month range (substr(due_date,1,7) = ym) and re-run it when the month changes, or aggregate server-side.

## apps/web/src/screens/Schranka.tsx  (3)

- **⚪ LOW** `Schranka.tsx:76` — Přeřazení úkolu do projektu je nevratné a bez potvrzení/undo [undo]
  - Scénář: reassign() přepíše project_id bez undo/toastu/potvrzení, zatímco schedule() undo nabízí (ř. 73) a doUndo() umí vrátit jen due_date. Po výběru necinboxového projektu ve <select> (ř. 148-165) úkol vypadne z filtru schránky (ř. 55-56) a zmizí z obrazovky, takže tam už opravu udělat nelze. Úkol ale není ztracen — je v cílovém projektu a lze ho vrátit zpět přes detail úkolu / Seznamy. Sjednotit undo mechaniku s schedule (uchovat předchozí project_id a nabídnout „Vrátit zpět").
  - Oprava: Přidat undo i pro reassign (uchovat předchozí project_id a nabídnout „Vrátit zpět" v toastu), případně potvrzení; sjednotit s undo mechanikou schedule.
- **⚪ LOW** `Schranka.tsx:205` — Undo toast schránky se nikdy sám neschová (bez timeoutu i zavření) [state]
  - Scénář: Po schedule() se zobrazí fixní undo lišta (ř. 205-226). Nemá setTimeout ani křížek — zmizí jen když uživatel klikne „Vrátit zpět" (což akci vrátí, i když ji nechtěl vrátit) nebo když naplánuje další úkol (setUndo přepíše). Uživatel spokojený s naplánováním má trvale visící lištu přes obsah, dokud neodejde z obrazovky.
  - Oprava: Přidat auto-dismiss (např. 5 s) a/nebo tlačítko zavřít, které jen skryje toast bez vrácení akce.
- **⚪ LOW** `Schranka.tsx:63` — Odškrtnutí ve schránce volá toggleTask bez actorId → obchází R2 shared_all [invariant]
  - Scénář: User quick-adds a task without #project but with 2+ @people (e.g. "Napsat report @adam @petr") and no due date. Per QuickAdd.tsx:147/166 the task is created in the personal inbox with assignment_mode='shared_all' and two assignments, due_date=null. It appears in Schránka (Schranka.tsx:50 query). The user clicks its round checkbox → toggle (Schranka.tsx:63) → toggleTask(tk) without actorId. The guard at tasks.ts:135 is false, the R2 shared_all branch is skipped, and the general branch (tasks.ts:276 writeDone) sets tasks.completed_at for the whole task — one member completes the shared task for all, bypassing derived per-person completion, while assignments.completed_at stay null (inconsistent). Fix: pass the current user id, toggleTask(tk, session.user.id), matching all other toggle call sites.
  - Oprava: Předat aktuálního uživatele: toggleTask(tk, session.user.id), stejně jako to dělají ostatní obrazovky.

## apps/web/src/components/TasksToolbar.tsx  (2)

- **⚪ LOW** *(uncertain)* `TasksToolbar.tsx:147` — Řazení „dle termínu" sestupně vyhodí úkoly bez termínu nahoru místo dolů [correctness]
  - Scénář: sortBy="due", asc=false: tasks without a due_date appear at the top of the list rather than the bottom, because byDue treats null as max-value and dir=−1 flips the comparator. This is consistent sort semantics (null = infinity); it only counts as a bug if the product spec/prototype requires no-date tasks pinned to the bottom regardless of direction, which is not established in the code.
  - Oprava: Řadit null-termíny mimo směr: v byDue vrátit hodnotu, kterou dir neotočí (např. porovnávat jen ne-null a nully vždy dávat na konec před aplikací dir), nebo přičíst směr jen na porovnání dat.
- **⚪ LOW** `TasksToolbar.tsx:187` — Filtr osoby zůstává aktivní a tiše filtruje i po přepnutí do osobního (soukromého) workspace [correctness]
  - Scénář: V týmovém ws nastavím filtr osoby (state.people neprázdné) → přepnu přes sidebar do osobního ws. setActiveWs mění jen context (žádný remount), tb v useState obrazovky přetrvá; sekce Osoba v dropdownu zmizí (showPersonFilter=false), ale filterTasks (ř. 187) people dál aplikuje. Úkoly se dál filtrují dle přiřazení — u filtru na konkrétního kolegu se osobní úkoly (bez tohoto přiřazení) odfiltrují až na prázdný seznam. NENÍ to však tiché: aktivní chip osoby zůstává viditelný nahoře s křížkem (ř. 335-339), tlačítko Filtr svítí a v dropdownu je „Vymazat filtry" (ř. 525) — uživatel má viditelný indikátor i dvě cesty k nápravě; skryté jsou jen jednotlivé person-pilulky. Oprava dle návrhu (ve filterTasks people ignorovat při showPersonFilter=false, případně people vyprázdnit při přepnutí ws) je namístě.
  - Oprava: Při showPersonFilter=false lidský filtr ignorovat ve filterTasks, nebo people vyprázdnit při přepnutí workspace.

## apps/web/src/layout/Sidebar.tsx  (2)

- **⚪ LOW** `Sidebar.tsx:389` — Rozbalovací šipka workspace je klikací cíl ~9px (pod 24px) [a11y]
  - Scénář: Tlačítko pro sbalení/rozbalení pracovního prostoru obsahuje jen 9×9 SVG s className 'flex' bez paddingu → efektivní klikací plocha ~9px, hluboko pod doporučenými 24px. Trefit rozbalení/sbalení prostoru je obtížné, zvlášť na trackpadu/dotyku.
  - Oprava: Zvětšit hit target tlačítka na min. 24×24px (padding nebo pevná šířka/výška se zarovnaným SVG).
- **⚪ LOW** `Sidebar.tsx:153` — Inbox názvy natvrdo duplikované místo použití sdíleného INBOX_NAMES → riziko rozjetí počtů Schránky [correctness]
  - Scénář: Sidebar (Sidebar.tsx:153-159) počítá inbox množinu z natvrdo zapsaných literálů `p.name === "Doručené" || p.name === "Inbox"` místo sdíleného INBOX_NAMES / inboxProjectIds z lib/inbox.ts, které používají všechny odpovídající obrazovky (Header, Today, Schránka, Velín, Přehled, Úkoly). Hodnoty jsou dnes identické → žádná aktuální divergence počtů. Latentní riziko: jakmile někdo přidá/lokalizuje název schránky v INBOX_NAMES (nebo naopak jen v Sidebaru), badge /schranka, /dnes, /nadchazejici a /ukoly se rozejdou s obsahem obrazovek. Oprava: importovat a použít INBOX_NAMES/inboxProjectIds/isInboxTask; totéž platí pro duplicitní literály v Hledat.tsx:14 a AddTaskModal.tsx:305.
  - Oprava: V Sidebaru importovat a použít INBOX_NAMES z lib/inbox.ts (a ideálně pickInboxId/isInboxTask) místo lokálních literálů.

## apps/web/src/lib/occurrences.ts  (1)

- **⚪ LOW** `occurrences.ts:161` — Guard 800 iterací od base: denní opakování navigované >~2,2 roku od base zmizí (okno se nikdy nedosáhne) [correctness]
  - Scénář: V kalendáři (Calendar.tsx, měsíční režim) opakovaně klikni na šipku „další měsíc" (shiftCur(1), ř. 526) tak, aby zobrazený měsíc začínal více než ~800 dní (≈2,19 roku) po due_date/base denně opakovaného úkolu (kind="daily"). expandOccurrences krokuje k od base po 1 dni; guard 800 se vyčerpá dřív, než cur (=base+k) dosáhne začátku okna fromISO, a protože cur zároveň nikdy nepřekročí konec okna toISO (je ještě dál), nespadne ani časný break — vrátí se prázdné pole a denní opakování se v tak vzdáleném měsíci vůbec nevykreslí. Weekly/biweekly/monthly/yearly nejsou dotčeny (800 kroků pokryje 15+ let). Oprava: pro krokovatelné druhy předpočítat počáteční k skokem k fromISO místo lineárního krokování od base, nebo škálovat guard dle kroku druhu.
  - Oprava: Pro krokovatelné druhy (daily/weekly/biweekly/monthly/yearly) předpočítat počáteční `k` skokem k `fromISO` místo lineárního krokování od base, nebo guard škálovat dle druhu.

## apps/web/src/lib/chainReflow.ts  (1)

- **⚪ LOW** `chainReflow.ts:111` — shiftChain ignores skip_weekend, so ±1d can park steps on a weekend despite 'Bez víkendů' [correctness]
  - Scénář: With 'Bez víkendů' enabled (skip_weekend=1), the −1d/+1d planning buttons call shiftChain (Postupy line 789/799), which raw-shifts every due by delta (line 118-123) and never consults skip_weekend or calls nextWork(). A Monday chain shifted −1d lands on Sunday, contradicting the weekend-skipping invariant the toggle promises and that reflowChain otherwise enforces.
  - Oprava: After shifting, apply nextWork() to each due when chain.skip_weekend is set (or route the shift through reflowChain), so weekend-free scheduling holds for manual shifts too.

## apps/web/src/screens/Nastaveni.tsx  (1)

- **⚪ LOW** `Nastaveni.tsx:407` — Menu změny role se nezavře klikem mimo ani Esc [overlay-esc]
  - Scénář: Otevřu dropdown role u člena (klik na chevron, ř. 407–410). Kliknu jinam na stránku nebo zmáčknu Esc → menu (absolutní overlay, zIndex 6, ř. 449–463) zůstane otevřené a překrývá obsah. Zavřít bez změny role lze jen opětovným klikem na tentýž chevron; výběr role menu sice zavře (setRole → setOpenRoleId(null), ř. 136), ale změní roli. Chybí mousedown-outside i Escape handler, na rozdíl od InviteModalu (Esc handler ř. 773–779) a BulkBaru.
  - Oprava: Přidat mousedown-outside a Escape listener, který nastaví setOpenRoleId(null).

## apps/web/src/lib/workspace.tsx  (1)

- **⚪ LOW** `workspace.tsx:81` — Přepnutí prostoru nesbalí dříve ručně rozbalený prostor → víc rozbalených prostorů zároveň [state]
  - Scénář: Switch active workspace two or more times via setActiveWs (e.g. A→B→C), or manually expand a non-active workspace via toggleCollapse and then switch away. Because setActiveWs writes a permanent explicit collapsed[id]=false for each newly-activated workspace and never collapses the prior one, all previously-activated (or manually-expanded) workspaces remain expanded in the sidebar simultaneously, contradicting the isCollapsed default (only the active workspace expanded). Cosmetic only; state is in-memory and resets on page reload. Fix: on setActiveWs collapse the previous active / clear stale explicit falses, or set collapsed[previous]=true.
  - Oprava: Při setActiveWs buď resetovat collapsed (vyčistit explicitní stavy), nebo nastavit collapsed[predchozi]=true, aby zůstal rozbalený jen aktivní prostor.

## apps/web/src/mail/AdminScreen.tsx  (1)

- **⚪ LOW** `AdminScreen.tsx:597` — Matice Přístupů (lokální cache) a karta osoby (m.adm.acc ze seedu) se rozcházejí [sync]
  - Scénář: V Administraci pošty otevři matici Přístupů. Klikni na buňku (schránka × osoba), čímž se role přepne (cyklus 0→1→2) — zápis jde do modulové `cache.adm.acc` v AdminScreen.tsx. Poté klikni na avatar téže osoby v hlavičce sloupce (data-pcol → setPerson(pid)); PersonCard čte `m.adm.acc` z provideru, který drží pouze hodnoty ze seedu (ADM_SEED.acc, nikdy přepsané), takže sekce SCHRÁNKY ukáže PŮVODNÍ roli. Změna z matice je tak neviditelná v místě, kde uživatel roli osoby ověřuje. Vše je ale demo bez persistence.
  - Oprava: Přesunout acc do provideru (vzor S5 pro fixed/ai) a číst z `m.adm.acc` na obou místech, nebo PersonCard napojit na tutéž cache jako matice.

## apps/web/src/mail/NastaveniScreen.tsx  (1)

- **⚪ LOW** `NastaveniScreen.tsx:176` — VIP: duplicitní adresa → kolize React key + odebrání smaže obě [empty-state]
  - Scénář: V Nastavení → Mail → sekce VIP odesílatelé napiš do inputu `a@b.cz`, stiskni ⏎, pak znovu `a@b.cz` a ⏎. Vzniknou dva chipy se stejným `key="a@b.cz"` (React varuje o duplicitních klíčích). Klik na × u kteréhokoli z nich zavolá `filter((z) => z !== "a@b.cz")` a smaže oba naráz.
  - Oprava: V addVip odmítnout duplicitu (`if (nast.vip.includes(v)) return;`) a normalizovat na lowercase/trim; případně mazat podle indexu, ne podle hodnoty.

## apps/web/src/mail/AskWatson.tsx  (1)

- **⚪ LOW** `AskWatson.tsx:95` — Router odpovědí: klíčové slovo „napiš/odpověz" přebije věcné dotazy do větve draftů [correctness]
  - Scénář: Uživatel napíše do Ask Watson volný dotaz obsahující imperativ „napiš/odpověz" spolu s věcným klíčem, např. „napiš mi kolik zbývá za nájem" nebo „odpověz jaká je poslední faktura". Regex na ř. 95 chytne „napiš"/„odpově(z)" a dotaz spadne do větve hromadných draftů (ř. 95–128), takže se vrátí hláška o přípravě návrhů odpovědí místo věcné odpovědi o faktuře (ř. 156). Věcné větve (faktura/grant/MČR) jsou až za obecnou draftovou větví, takže je obecné sloveso přebije.
  - Oprava: Zpřísnit regex draftů (např. vyžadovat slovo „draft(y)" nebo „drafty na …"), nebo pořadí větví změnit tak, aby konkrétní věcné klíče (faktura/grant/MČR) měly přednost před obecným „napiš".

## apps/web/src/lib/rowMeta.tsx  (1)

- **⚪ LOW** `rowMeta.tsx:78` — Avatary přiřazených z jiného prostoru se zobrazí jako '?' (team scoped na activeWs, asg globální) [correctness]
  - Scénář: Uživatel je členem ≥2 prostorů (A i B) s neshodným členstvím. V aktivním prostoru A otevře nescopovanou obrazovku Úkoly (Ukoly.tsx:50-51, SELECT * FROM tasks bez ws filtru), Nadcházející nebo Oblíbené. Úkol patřící prostoru B, přiřazený uživateli, který je členem jen B (ne A), se vykreslí přes TaskItem → metaOf → meta.avatars; jeho iniciály jsou "?" (rowMeta.tsx:106), protože nameMap se plní jen ze členů activeWs=A (ř. 78-88). Pilulka „Každý zvlášť N/M" počítá správně, nečitelná je jen identita lidí. Jde o degradaci čitelnosti avatarů v agregovaných cross-workspace seznamech, bez dopadu na správnost dat či bezpečnost. Oprava: řešit jména členů napříč všemi prostory (existující useAllMembers z lib/overview.ts) místo jen activeWs. Dílčí bod o neomezené cache je nepodstatný — Map se invaliduje s daty přes useMemo deps.
  - Oprava: Načítat jména členů napříč všemi prostory uživatele (nebo globální members endpoint) místo jen activeWs; pro cache zvážit horní mez / WeakMap.
---

## Vyřešení — 2026-07-12 (commity c48c248, f6e0873, 7986a94)

Opravy provedeny multi-agentním fix workflow (14 disjunktních klastrů) +
centrální dořešení. **~125 nálezů opraveno** (všechny HIGH + většina MED +
bezpečné LOW). Gates po opravách: `tsc` celý workspace čistý, occurrences
14/14, quickadd corpus 321/321 (aktualizován stale case „za N dní" + přidán
kanonický), prettier čistý, všech 12 rout renderuje bez error-boundary.

### Přeskočeno / odloženo (16), s důvodem
Většina jsou **falešné poplachy** (audit sám značil `uncertain`, nebo kód už
guard měl) a subjektivní/kosmetické LOW:
- **Falešné pro daný soubor** (guard už existuje): kbNav.tsx:29 zkratky vs.
  popover (skutečný fix doplněn v TasksToolbar — viz níže); BulkBar.tsx:153
  Esc (guard `openId || [data-esc-layer]` už je).
- **Uncertain / produktové rozhodnutí**: duplikace opakovaného úkolu a
  doneCount; řazení „dle termínu" null=∞; dvojité odeslání v MailPeek
  (React odmountuje tlačítko dřív).
- **Vyžaduje širší změnu než LOW**: CalendarWidget filtr firmy + limit dotazu
  (kříží Prehled/PeekPanel); Velin urgentní KPI z ořezaného digestu; undo
  toast 5→10 s (lib/toast.tsx); handoffName offline; chain manual gate;
  Calendar dotykové cíle (1:1 s Cloud Design).
- **Záměrný port prototypu**: Seznamy reset odarchivuje.

### Dořešeno centrálně nad rámec klastrů (commit 7986a94)
- **MED** TasksToolbar Filtr/Řazení popover → `data-esc-layer` + Esc zavře
  (skutečný nositel „zkratky vs. popover").
- **MED** state.doSend připojí zvolený podpis (SigBlock) při odeslání.

### Deferred backlog (koncepční, mimo tuto dávku)
- undo řetězců postupů vyžaduje 2× ⌘Z (sloučení patří do toggleTask/tasks.ts).
- CalendarWidget filtr firmy + limit; Velin urgentní KPI sladit s NotifCenter.
- undo action-toast TTL 10 s (lib/toast.tsx).
